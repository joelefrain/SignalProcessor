# Escalamiento sísmico y ajuste espectral de registros de terremoto con rigor científico

**Enfoque:** ingeniería sísmica geotécnica y estructural.  
**Objetivo:** enseñar a seleccionar, escalar y modificar registros acelerográficos para que sean compatibles con un espectro objetivo, sin perder el control físico sobre amplitud, contenido frecuencial, duración, energía, no estacionariedad y respuesta elástica e inelástica.  
**Alcance:** registros reales, escalamiento lineal, selección por espectro objetivo, ajuste espectral en dominio del tiempo mediante wavelets, métodos CWT/DWT, criterios de aceptación, control de calidad y consideraciones geotécnicas de sitio.

---

## 1. Resumen ejecutivo

El escalamiento sísmico no es simplemente multiplicar un acelerograma por un factor. Es un procedimiento de ingeniería que debe responder, como mínimo, a estas preguntas:

1. **¿Qué amenaza se quiere representar?**  
   Un espectro de diseño normativo, un espectro uniforme de amenaza, un espectro condicional, un escenario de magnitud-distancia, un espectro de respuesta de sitio, o una combinación de estos.

2. **¿Dónde se define el movimiento?**  
   Superficie libre, afloramiento rocoso, movimiento "within" a cierta profundidad, base de un modelo geotécnico, cimentación, o interfaz suelo-estructura.

3. **¿Qué respuesta se controlará?**  
   Respuesta elástica de 5% de amortiguamiento, respuesta sobreamortiguada, respuesta inelástica, demanda de deformación, demanda de aceleración de piso, licuefacción, deformación permanente, estabilidad sísmica, o análisis de desempeño.

4. **¿Qué propiedades del registro se deben preservar?**  
   Forma temporal, no estacionariedad, polaridad, duración significativa, intensidad de Arias, contenido de Fourier, pulsos de directividad, coherencia horizontal, relación vertical-horizontal y compatibilidad entre componentes.

5. **¿Qué error espectral es aceptable?**  
   La tolerancia no debe definirse solo como un porcentaje de coincidencia espectral. También debe verificarse que el registro ajustado siga siendo físicamente plausible.

El procedimiento técnicamente robusto es:

- definir el **espectro objetivo** y el **rango de periodos de interés**;
- seleccionar registros semilla con magnitud, distancia, mecanismo, sitio, duración y forma espectral coherentes;
- preprocesar los registros de forma trazable;
- calcular espectros elásticos e indicadores de intensidad;
- aplicar escalamiento lineal o ajuste espectral según el objetivo;
- verificar convergencia espectral y preservar parámetros físicos;
- documentar todos los factores, cambios y criterios de aceptación.

---

## 2. Vocabulario mínimo

**Acelerograma:** serie temporal de aceleración del suelo, usualmente $a_g(t)$.

**Registro semilla:** acelerograma real antes de escalar o modificar.

**Escalamiento lineal:** multiplicación de todo el registro por un factor constante $s$. Preserva fase, duración relativa y contenido frecuencial normalizado, pero cambia todas las medidas dependientes de amplitud.

**Ajuste espectral / spectral matching:** modificación del registro para que su espectro de respuesta coincida con un espectro objetivo en un rango de periodos. Puede hacerse en dominio de frecuencia, dominio del tiempo, o mediante transformadas wavelet.

**Espectro objetivo:** curva $S_{a,\mathrm{tar}}(T,\xi)$, $S_{d,\mathrm{tar}}(T,\xi)$ u otra medida espectral que representa la demanda sísmica de diseño o evaluación.

**UHS:** Uniform Hazard Spectrum. Espectro construido con ordenadas que tienen la misma probabilidad anual de excedencia en cada periodo. Puede ser conservador como objetivo para registros individuales, porque combina ordenadas extremas que no necesariamente ocurren juntas en un solo terremoto.

**CMS / CS:** Conditional Mean Spectrum / Conditional Spectrum. Espectros condicionados a una intensidad en un periodo $T^*$. Suelen ser más consistentes con un escenario probabilístico cuando el análisis se condiciona a $S_a(T^*)$.

**RotD50 / RotD100:** medidas espectrales independientes de orientación, obtenidas rotando dos componentes horizontales y tomando percentiles de la respuesta.

**Duración significativa:** intervalo en el que se acumula un porcentaje de la intensidad de Arias, típicamente $D_{5-95}$ o $D_{5-75}$.

**No estacionariedad:** variación temporal de amplitud y contenido frecuencial. Los terremotos reales no son señales estacionarias; su energía y frecuencia dominante cambian en el tiempo.

---

## 3. Información necesaria antes de escalar registros

### 3.1 Información de amenaza y sitio

Se requiere:

- ubicación del proyecto;
- tipo tectónico: cortical activo, subducción interplaca, intraplaca, estable continental;
- magnitudes dominantes de la amenaza;
- distancias dominantes: $R_{rup}$, $R_{jb}$, $R_{hyp}$, según el modelo usado;
- mecanismo focal: normal, inverso, transcurrente;
- parámetros de sitio: $V_{S30}$, perfil $V_S(z)$, profundidad a roca ingenieril, $Z_{1.0}$, $Z_{2.5}$, clase de sitio;
- condición del movimiento: afloramiento, superficie, roca, base de columna de suelo;
- espectro objetivo y nivel de amenaza: SLE, DBE, MCE, MCER, OBE, SSE, periodo de retorno o probabilidad de excedencia;
- si el análisis será lineal, equivalente lineal, no lineal, SSI, licuefacción, estabilidad, deformación permanente o respuesta estructural.

### 3.2 Información de la estructura o sistema geotécnico

Se requiere:

- periodos fundamentales $T_1$ en las direcciones principales;
- periodos de modos superiores relevantes;
- rango esperado de elongación de periodo por no linealidad;
- nivel esperado de ductilidad o degradación cíclica;
- si hay aislamiento, disipadores, interacción suelo-estructura, cimentaciones profundas, presas, depósitos de relaves, taludes, túneles o estructuras enterradas;
- demanda crítica: desplazamiento, aceleración, deformación cortante, esfuerzo cíclico, presión de poros, asentamiento, estabilidad global.

### 3.3 Información del registro

Para cada registro deben conocerse:

- evento, estación y componente;
- magnitud, distancia, mecanismo, profundidad, directividad;
- $V_{S30}$ de estación y clasificación de sitio;
- intervalo de muestreo $\Delta t$;
- unidades;
- procesamiento previo: corrección de línea base, filtros, ventanas, pads, taper;
- rango de periodos confiable del registro;
- PGA, PGV, PGD;
- $S_a(T)$, $S_v(T)$, $S_d(T)$;
- intensidad de Arias;
- duración significativa;
- espectro de Fourier;
- posible presencia de pulso de velocidad.

---

## 4. Preprocesamiento de acelerogramas

El preprocesamiento es una etapa crítica. Un registro mal corregido puede coincidir con el espectro objetivo y aun así producir desplazamientos irreales, energía falsa o sesgo en análisis no lineales.

### 4.1 Unidades

Convención recomendada:

- trabajar internamente en SI: $a$ en $\mathrm{m/s^2}$, $v$ en $\mathrm{m/s}$, $d$ en $\mathrm{m}$;
- si el registro está en $g$, convertir:
  $$
  a_{\mathrm{SI}}(t)=g\,a_g(t), \quad g=9.80665\ \mathrm{m/s^2}
  $$
- si el espectro objetivo está en $g$, mantener consistencia:
  $$
  S_{a,g}(T)=\frac{S_{a,\mathrm{SI}}(T)}{g}
  $$

### 4.2 Corrección de línea base

Un acelerograma con offset pequeño puede generar velocidades y desplazamientos artificiales grandes al integrar. Una línea base contaminada afecta especialmente:

- desplazamiento permanente;
- periodos largos;
- $S_d(T)$;
- espectros de estructuras flexibles;
- análisis de taludes, presas y deformación permanente.

Correcciones comunes:

1. **Remoción de media:**  
   $$
   a_c(t)=a(t)-\bar a
   $$

2. **Corrección polinómica:**  
   ajustar $a(t)$, $v(t)$ o $d(t)$ con polinomios de bajo orden y sustraer la tendencia.

3. **Corrección por segmentos:**  
   útil cuando hay offset inducido por el instrumento o por rotación/tilt.

4. **Taper inicial/final:**  
   reduce discontinuidades antes de filtrar, pero puede eliminar contenido real si se aplica agresivamente.

5. **Filtros pasa-altas/pasa-bajas:**  
   deben justificarse mediante relación señal-ruido y rango de periodos de interés.

### 4.3 Filtrado

El filtrado no debe ser un paso automático. Debe responder a una pregunta: ¿qué rango de frecuencias del registro es confiable?

- **Filtro pasa-altas:** elimina deriva de largo periodo, pero puede eliminar desplazamiento real.
- **Filtro pasa-bajas:** elimina ruido de alta frecuencia, pero puede afectar PGA y respuesta de periodos cortos.
- **Filtro causal:** introduce cambio de fase.
- **Filtro acausal:** evita desfase, pero requiere pads suficientes y cuidado al recortar.

Criterio práctico:

- conservar el registro solo en el rango de periodos donde la relación señal-ruido es aceptable;
- documentar $f_{low}$, $f_{high}$, orden del filtro, tipo de filtro, aplicación causal/acausal;
- no usar ordenadas espectrales más allá del periodo máximo confiable.

### 4.4 Re-muestreo

El intervalo $\Delta t$ debe capturar adecuadamente los periodos más cortos. Reglas prácticas:

- usar al menos 10 a 20 pasos por ciclo del periodo mínimo de interés:
  $$
  \Delta t \leq \frac{T_{\min}}{10\text{ a }20}
  $$
- verificar Nyquist:
  $$
  f_N=\frac{1}{2\Delta t}
  $$
- no interpolar para fabricar contenido de alta frecuencia inexistente.

---

## 5. Parámetros de intensidad y duración

### 5.1 PGA, PGV, PGD

$$
PGA=\max_t |a_g(t)|
$$

$$
PGV=\max_t |v_g(t)|
$$

$$
PGD=\max_t |d_g(t)|
$$

donde:

$$
v_g(t)=\int_0^t a_g(\tau)\,d\tau
$$

$$
d_g(t)=\int_0^t v_g(\tau)\,d\tau
$$

PGV y PGD son muy sensibles a línea base y filtrado.

### 5.2 Intensidad de Arias

La intensidad de Arias mide energía acumulada de aceleración:

$$
I_A(t)=\frac{\pi}{2g}\int_0^t a_g^2(\tau)\,d\tau
$$

donde $a_g(\tau)$ debe estar en $\mathrm{m/s^2}$. El valor total es:

$$
I_A=I_A(t_{\mathrm{end}})
$$

Unidades:

$$
[I_A]=\mathrm{m/s}
$$

Si la aceleración está expresada en unidades de $g$, como $a^*(t)=a(t)/g$, entonces:

$$
I_A(t)=\frac{\pi g}{2}\int_0^t \left(a^*(\tau)\right)^2 d\tau
$$

**Efecto del escalamiento lineal:** si $a_s(t)=s\,a(t)$, entonces:

$$
I_{A,s}=s^2 I_A
$$

Esto es importante: un factor lineal 2 cuadruplica la intensidad de Arias.

### 5.3 Husid plot

La curva de Husid es la intensidad de Arias normalizada:

$$
H(t)=\frac{I_A(t)}{I_A(t_{\mathrm{end}})}
$$

Sirve para visualizar cuándo se acumula la energía del movimiento.

### 5.4 Duración significativa

Para un porcentaje $p$, se define $t_p$ tal que:

$$
I_A(t_p)=p\,I_A(t_{\mathrm{end}})
$$

Entonces:

$$
D_{5-95}=t_{95}-t_5
$$

$$
D_{5-75}=t_{75}-t_5
$$

La duración significativa debe preservarse especialmente en:

- licuefacción;
- degradación cíclica;
- análisis no lineal de suelos;
- estructuras con deterioro de resistencia o rigidez;
- presas y relaves;
- sistemas sensibles a acumulación de deformación.

### 5.5 CAV y CAV estándar

La velocidad absoluta acumulada:

$$
CAV=\int_0^{t_{\mathrm{end}}}|a_g(t)|\,dt
$$

El $CAV_{STD}$ usa ventanas de 1 segundo y umbrales de aceleración. Es útil como medida de daño acumulado y filtrado de movimientos débiles.

### 5.6 Intensidades espectrales

**Housner intensity:**

$$
I_H=\int_{T_a}^{T_b} PSV(T,\xi)\,dT
$$

con rangos típicos de $0.1$ a $2.5$ s, según aplicación.

**Acceleration Spectrum Intensity:**

$$
ASI=\int_{T_a}^{T_b} S_a(T,\xi)\,dT
$$

**Velocity Spectrum Intensity:**

$$
VSI=\int_{T_a}^{T_b} PSV(T,\xi)\,dT
$$

Estas medidas ayudan a verificar que un ajuste espectral no haya alterado excesivamente la energía relevante para una banda modal.

---

## 6. Respuesta elástica de un oscilador SDOF

La ecuación de movimiento relativa para un oscilador lineal de un grado de libertad sometido a aceleración de base es:

$$
m\ddot u(t)+c\dot u(t)+ku(t)=-m a_g(t)
$$

Dividiendo por $m$:

$$
\ddot u(t)+2\xi\omega_n\dot u(t)+\omega_n^2u(t)=-a_g(t)
$$

donde:

$$
\omega_n=\sqrt{\frac{k}{m}}, \quad T=\frac{2\pi}{\omega_n}, \quad \xi=\frac{c}{2m\omega_n}
$$

### 6.1 Espectro de desplazamiento

$$
S_d(T,\xi)=\max_t |u(t)|
$$

### 6.2 Pseudo-velocidad

$$
PSV(T,\xi)=\omega_n S_d(T,\xi)
$$

### 6.3 Pseudo-aceleración

$$
PSA(T,\xi)=\omega_n^2 S_d(T,\xi)
$$

En unidades de $g$:

$$
PSA_g(T,\xi)=\frac{\omega_n^2 S_d(T,\xi)}{g}
$$

### 6.4 Aceleración absoluta

La aceleración absoluta del oscilador es:

$$
a_{\mathrm{abs}}(t)=\ddot u(t)+a_g(t)
$$

y el espectro de aceleración absoluta:

$$
S_{aa}(T,\xi)=\max_t |\ddot u(t)+a_g(t)|
$$

Para amortiguamientos pequeños, $PSA$ y $S_{aa}$ suelen ser cercanos, pero no son idénticos.

### 6.5 Integración numérica Newmark-$\beta$

El método de Newmark usa:

$$
u_{i+1}=u_i+\Delta t\,\dot u_i+\Delta t^2\left[\left(\frac12-\beta\right)\ddot u_i+\beta\ddot u_{i+1}\right]
$$

$$
\dot u_{i+1}=\dot u_i+\Delta t\left[(1-\gamma)\ddot u_i+\gamma\ddot u_{i+1}\right]
$$

El esquema de aceleración promedio constante usa comúnmente:

$$
\beta=\frac14,\quad \gamma=\frac12
$$

y es incondicionalmente estable para sistemas lineales. La precisión, sin embargo, depende de $\Delta t/T$. Para periodos cortos, si $\Delta t$ es grande, se requiere interpolación o subpasos.

---

## 7. Respuesta inelástica, ductilidad y espectros inelásticos

### 7.1 Ecuación general no lineal

$$
m\ddot u(t)+c\dot u(t)+F_s(u,z)=-m a_g(t)
$$

donde $F_s$ es la fuerza restauradora y $z$ representa variables internas de histéresis, plasticidad o degradación.

### 7.2 Modelo elastoplástico bilineal

Antes de fluencia:

$$
F_s=ku
$$

La fluencia ocurre en:

$$
F_y=ku_y
$$

Después de fluencia, con razón de endurecimiento $\alpha$:

$$
F_s=F_y+\alpha k(u-u_y)
$$

si la carga es monotónica positiva. En carga cíclica debe implementarse una regla histérica: elastoplástica perfecta, bilineal cinemática, Takeda, Bouc-Wen, Clough, Ibarra-Krawinkler, etc.

### 7.3 Ductilidad

La ductilidad de desplazamiento se define como:

$$
\mu=\frac{u_{\max}}{u_y}
$$

donde:

- $u_{\max}$: desplazamiento máximo inelástico;
- $u_y$: desplazamiento de fluencia.

### 7.4 Factor de reducción por ductilidad

Si $S_e(T,\xi)$ es la demanda elástica de fuerza por unidad de masa y $S_y=F_y/m$, el factor de reducción es:

$$
R_\mu(T)=\frac{S_e(T,\xi)}{S_y(T,\mu)}
$$

Para construir un espectro de resistencia constante a ductilidad $\mu_t$:

1. fijar periodo $T$, amortiguamiento $\xi$, modelo histerético y $\alpha$;
2. elegir un valor inicial de $F_y$;
3. integrar la respuesta no lineal;
4. calcular $\mu=|u|_{\max}/u_y$;
5. iterar $F_y$ hasta que $\mu \approx \mu_t$;
6. registrar $S_y=F_y/m$.

### 7.5 Espectro inelástico

Un espectro inelástico puede expresarse como:

- espectro de resistencia $S_y(T,\mu)$;
- espectro de ductilidad $\mu(T,F_y)$;
- espectro de desplazamiento inelástico $S_{d,\mathrm{inel}}(T)$;
- espectro de energía o daño.

### 7.6 Implicancias para escalamiento

Dos registros pueden tener el mismo $S_a(T_1)$ elástico y producir respuestas inelásticas distintas por diferencias en:

- duración;
- número de ciclos;
- forma espectral alrededor de $T_1$;
- contenido de periodos largos por elongación;
- pulsos de velocidad;
- fase y no estacionariedad;
- energía acumulada.

Por eso no basta ajustar un solo periodo si el sistema entra en no linealidad importante.

---

## 8. Periodos de interés

El rango de periodos debe definirse antes de escalar.

### 8.1 Estructuras lineales o casi lineales

Rango mínimo:

$$
T_{\min}=0.2T_1,\quad T_{\max}=1.5T_1
$$

Este rango captura modos superiores y cierto alargamiento de periodo.

### 8.2 Estructuras no lineales

Cuando hay degradación o plastificación, el periodo efectivo se alarga. Puede usarse:

$$
T_{\max}=2T_1 \text{ a } 3T_1
$$

según sistema estructural, deformación esperada y sensibilidad modal.

### 8.3 Sistemas geotécnicos

En problemas geotécnicos el rango no se define solo por $T_1$ estructural. Deben considerarse:

- periodo fundamental del depósito:
  $$
  T_s \approx \frac{4H}{V_s}
  $$
  para un estrato equivalente sobre base rígida;
- periodos dominantes del espectro de respuesta superficial;
- frecuencias relevantes de licuefacción y degradación;
- frecuencias de modos de presas, taludes, relaves o túneles;
- frecuencia de corte del modelo numérico:
  $$
  \Delta t \leq \frac{1}{10 f_{\max}}
  $$

### 8.4 Componentes horizontales y verticales

Para análisis 2D/3D:

- escalar pares horizontales conjuntamente, no como señales independientes sin criterio;
- preservar orientación si hay directividad;
- usar RotD50 o RotD100 si la norma o el criterio de proyecto lo exige;
- definir si el componente vertical se escala con el mismo factor, con espectro vertical objetivo, o con razón $V/H$.

---

## 9. Espectros objetivo

### 9.1 Espectro normativo

Proviene de un código o norma. Ejemplos:

- ASCE/SEI 7;
- ASCE 41;
- Eurocode 8;
- NTE E.030;
- NCh433;
- NEC;
- normativas locales o espectros del proyecto.

Usualmente se define por parámetros de amenaza, sitio y nivel de diseño.

Ventaja: aplicabilidad normativa.  
Limitación: puede ser un envolvente conservador y no representar un escenario físico único.

### 9.2 Espectro uniforme de amenaza (UHS)

El UHS se obtiene de PSHA:

$$
P(S_a(T)>x)=p
$$

para cada periodo $T$, con la misma probabilidad de excedencia. Es útil para diseño, pero como objetivo para registros individuales puede mezclar contribuciones de distintos terremotos dominantes en distintos periodos.

### 9.3 Espectro condicional medio (CMS)

Se condiciona a una ordenada objetivo en un periodo $T^*$:

$$
\varepsilon^*=\frac{\ln S_a^*-\mu_{\ln S_a}(T^*)}{\sigma_{\ln S_a}(T^*)}
$$

La media condicional en otro periodo $T_i$ es:

$$
\mu_{\ln S_a(T_i)\mid S_a(T^*)}
=
\mu_{\ln S_a}(T_i)+
\rho_{i*}\sigma_{\ln S_a}(T_i)\varepsilon^*
$$

donde $\rho_{i*}$ es la correlación entre $\ln S_a(T_i)$ y $\ln S_a(T^*)$.

La desviación estándar condicional:

$$
\sigma_{\ln S_a(T_i)\mid S_a(T^*)}
=
\sigma_{\ln S_a}(T_i)\sqrt{1-\rho_{i*}^2}
$$

El CMS es apropiado cuando el análisis se condiciona a $S_a(T^*)$, por ejemplo en evaluación de una estructura cuyo EDP está altamente correlacionado con $S_a(T_1)$.

### 9.4 Espectro condicional completo (CS)

El CS no solo usa la media condicional, sino también la covarianza entre ordenadas espectrales. Se busca que el conjunto de registros reproduzca:

$$
\boldsymbol{\mu}_{\ln S_a|\ln S_a(T^*)}
$$

y

$$
\boldsymbol{\Sigma}_{\ln S_a|\ln S_a(T^*)}
$$

Es útil si se requiere distribución de respuestas, no solo media.

### 9.5 Espectros de sitio

Para geotecnia, el objetivo puede ser:

- espectro en roca de entrada;
- espectro de afloramiento;
- espectro en superficie;
- espectro compatible con análisis de respuesta de sitio;
- GMRS para instalaciones críticas;
- espectro de cimentación.

No se debe mezclar la ubicación del objetivo. Un movimiento compatible en superficie no es necesariamente compatible en base de columna de suelo.

---

## 10. Selección de registros semilla

### 10.1 Criterios principales

Seleccionar registros con compatibilidad en:

- magnitud;
- distancia;
- mecanismo focal;
- clase de sitio;
- tectónica;
- profundidad;
- directividad;
- duración;
- forma espectral;
- presencia o ausencia de pulso;
- rango de periodos confiable;
- calidad de procesamiento.

### 10.2 Criterio de forma espectral

El registro semilla debería requerir la mínima modificación razonable. Si el espectro del registro está muy lejos del objetivo, el ajuste puede introducir cambios no físicos.

Una métrica usual es el error logarítmico:

$$
E_i=\ln S_{a,i}^{record}(T)-\ln S_{a}^{target}(T)
$$

Para una serie de periodos $T_j$:

$$
SSE=\sum_{j=1}^{n} w_j
\left[
\ln S_{a}^{record}(T_j)-\ln S_{a}^{target}(T_j)
\right]^2
$$

### 10.3 Número de registros

El número depende del objetivo:

- para estimar respuesta media: al menos 7 registros es una práctica mínima frecuente;
- para estimar dispersión o fragilidad: se requieren muchos más, frecuentemente 20 a 30 o más;
- para análisis no lineal con gran dispersión: aumentar número de registros;
- para colapso: usar conjuntos amplios e incremental dynamic analysis si corresponde.

### 10.4 Registros cercanos a falla

En near-fault:

- identificar pulso de velocidad;
- preservar periodo del pulso $T_p$;
- orientar componentes fault-normal y fault-parallel si el análisis lo requiere;
- evitar ajustar a un espectro suave que destruya el pulso;
- seleccionar registros con $T_p$ compatible con magnitud/distancia y con el periodo del sistema.

### 10.5 Subducción

Para zonas de subducción considerar:

- eventos interplaca e intraplaca por separado;
- magnitudes grandes y duraciones largas;
- contenido de baja frecuencia;
- efectos de cuenca;
- fuerte acumulación de ciclos;
- posible diferencia entre registros corticales y de subducción aunque el espectro coincida.

---

## 11. Escalamiento lineal

### 11.1 Definición

$$
a_s(t)=s\,a(t)
$$

El espectro escala linealmente:

$$
S_{a,s}(T)=s\,S_a(T)
$$

$$
S_{v,s}(T)=s\,S_v(T)
$$

$$
S_{d,s}(T)=s\,S_d(T)
$$

Pero las medidas energéticas cuadráticas escalan como:

$$
I_{A,s}=s^2I_A
$$

### 11.2 Escalamiento a un periodo

Si se desea igualar en $T^*$:

$$
s=\frac{S_{a,\mathrm{tar}}(T^*)}{S_{a,\mathrm{rec}}(T^*)}
$$

Útil cuando la demanda está controlada por un periodo específico, pero insuficiente para sistemas con modos superiores o no linealidad.

### 11.3 Escalamiento por mínimos cuadrados lineales

Minimizar:

$$
J(s)=\sum_{j=1}^{n}w_j
\left[
sS_{a,\mathrm{rec}}(T_j)-S_{a,\mathrm{tar}}(T_j)
\right]^2
$$

Derivando:

$$
s=
\frac{\sum_j w_j S_{a,\mathrm{rec}}(T_j)S_{a,\mathrm{tar}}(T_j)}
{\sum_j w_j S_{a,\mathrm{rec}}^2(T_j)}
$$

### 11.4 Escalamiento por mínimos cuadrados logarítmicos

Minimizar:

$$
J(\ln s)=\sum_{j=1}^{n}w_j
\left[
\ln s+\ln S_{a,\mathrm{rec}}(T_j)-\ln S_{a,\mathrm{tar}}(T_j)
\right]^2
$$

Entonces:

$$
\ln s=
\frac{\sum_j w_j
\left[
\ln S_{a,\mathrm{tar}}(T_j)-\ln S_{a,\mathrm{rec}}(T_j)
\right]}
{\sum_j w_j}
$$

$$
s=\exp(\ln s)
$$

El enfoque logarítmico trata errores relativos de forma más equilibrada.

### 11.5 Escalamiento de un conjunto

Si se escala cada registro con su propio factor $s_k$, la media del conjunto puede ajustarse al objetivo, pero se reduce la dispersión si los factores son demasiado agresivos.

Media aritmética:

$$
\bar S_a(T_j)=\frac{1}{N}\sum_{k=1}^{N}s_kS_{a,k}(T_j)
$$

Media geométrica:

$$
\ln \bar S_{a,g}(T_j)=\frac{1}{N}\sum_{k=1}^{N}
\ln\left[s_kS_{a,k}(T_j)\right]
$$

Para espectros lognormales, la media geométrica suele ser más coherente estadísticamente.

### 11.6 Restricción de factores de escala

No existe un límite universal, pero factores muy altos o bajos deben justificarse. Reglas de ingeniería frecuentes:

- preferible $0.5 \le s \le 2.0$;
- aceptable en algunos casos $0.25 \le s \le 4.0$ si la forma espectral y escenario son coherentes;
- evitar factores extremos si se altera de forma no realista PGA, PGV, Arias, duración efectiva o potencial de daño.

---

## 12. Ajuste espectral

### 12.1 Cuándo usarlo

Usar ajuste espectral cuando:

- la normativa exige compatibilidad estricta con un espectro objetivo;
- se requiere reducir dispersión para análisis no lineal intensivo;
- hay pocos registros reales adecuados;
- se analiza una instalación crítica;
- se quiere controlar varios periodos simultáneamente.

Evitarlo o usarlo con cautela cuando:

- el registro tiene pulso near-fault que debe preservarse;
- se requiere estimar dispersión natural de respuesta;
- se estudian efectos de duración o daño acumulado y el ajuste modifica significativamente la energía;
- el objetivo es construir curvas de fragilidad con variabilidad realista;
- el espectro objetivo es un UHS muy conservador y se ajusta cada registro exactamente, eliminando dispersión.

### 12.2 Error espectral

Error relativo:

$$
e(T_j)=\frac{S_{a,\mathrm{mod}}(T_j)-S_{a,\mathrm{tar}}(T_j)}
{S_{a,\mathrm{tar}}(T_j)}
$$

Error logarítmico:

$$
\eta(T_j)=\ln\left[
\frac{S_{a,\mathrm{mod}}(T_j)}
{S_{a,\mathrm{tar}}(T_j)}
\right]
$$

Máximo error:

$$
\eta_{\max}=\max_j |\eta(T_j)|
$$

Error RMS:

$$
\eta_{RMS}=
\sqrt{
\frac{\sum_j w_j\eta^2(T_j)}
{\sum_j w_j}
}
$$

Una tolerancia razonable debe definir:

- rango de periodos;
- amortiguamiento;
- métrica de error;
- tolerancia máxima;
- tolerancia media o RMS;
- criterio por registro o por media del conjunto;
- qué hacer con registros no convergentes.

### 12.3 Tolerancias típicas

Criterios usuales:

- ajuste individual estricto: $|e(T)|\le 5\%$ en el rango;
- ajuste individual moderado: $|e(T)|\le 10\%$;
- media de conjunto normativa: no menor que 90% del objetivo en el rango definido;
- evitar exceso sistemático grande si se busca compatibilidad y no sobredimensionamiento.

No se recomienda aceptar un ajuste solo por cumplir tolerancia espectral. Debe pasar control de calidad físico.

---

## 13. Métodos de ajuste espectral

### 13.1 Método en dominio de frecuencia

Procedimiento básico:

1. calcular FFT del registro;
2. modificar amplitudes de Fourier;
3. conservar o ajustar fases;
4. transformar de vuelta al dominio del tiempo;
5. iterar hasta compatibilidad espectral.

Ventaja:

- implementación relativamente sencilla;
- control directo del contenido frecuencial promedio.

Limitaciones:

- puede alterar severamente la no estacionariedad;
- puede generar precursores o energía artificial;
- puede afectar desplazamientos;
- no localiza cambios en el tiempo.

### 13.2 Método en dominio del tiempo con wavelets

Modifica el registro sumando funciones localizadas en tiempo y frecuencia:

$$
a_{mod}(t)=a(t)+\Delta a(t)
$$

$$
\Delta a(t)=\sum_{j=1}^{N} b_j f_j(t)
$$

donde:

- $f_j(t)$: wavelet de ajuste asociada a un periodo/frecuencia;
- $b_j$: amplitud de la wavelet;
- $N$: número de periodos objetivo.

El objetivo es modificar selectivamente las ordenadas espectrales sin reescribir toda la señal.

### 13.3 Método RspMatch / Lilhanand-Tseng / Abrahamson

Para cada periodo $T_i$, frecuencia $\omega_i$ y amortiguamiento $\xi_i$, se calcula:

- respuesta del oscilador;
- valor espectral actual $R_i$;
- valor objetivo $Q_i$;
- tiempo de pico $t_i$;
- polaridad $P_i$.

El desajuste espectral:

$$
\Delta R_i=(Q_i-R_i)P_i
$$

La respuesta al ajuste $\Delta a(t)$ en el tiempo de pico se aproxima por:

$$
\Delta R_i=\sum_{j=1}^{N} C_{ij}b_j
$$

donde:

$$
C_{ij}=\int_0^{t_i} f_j(\tau)h_i(t_i-\tau)d\tau
$$

y $h_i(t)$ es la función de respuesta impulsiva del oscilador.

En forma matricial:

$$
\Delta \mathbf{R}=\mathbf{C}\mathbf{b}
$$

$$
\mathbf{b}=\mathbf{C}^{-1}\Delta\mathbf{R}
$$

En la práctica se usa SVD, regularización o reducción de términos fuera de la diagonal para evitar inestabilidad numérica.

El registro se actualiza con relajación:

$$
a^{(r+1)}(t)=a^{(r)}(t)+\lambda \Delta a^{(r)}(t)
$$

con:

$$
0<\lambda\le 1
$$

y se repite hasta cumplir tolerancia.

### 13.4 Supuesto clave

El método asume que el tiempo del pico de respuesta $t_i$ no cambia significativamente al agregar una wavelet pequeña. Por eso:

- se usan iteraciones;
- se aplica relajación;
- se limita la amplitud del ajuste;
- se seleccionan registros semilla cercanos al objetivo;
- se divide el ajuste en barridos de periodos.

### 13.5 Wavelet de coseno atenuado

Una forma típica:

$$
f_j(t)=
\cos\left[\omega'_j(t-t_j+\Delta t_j)\right]
\exp\left[-\left(\frac{t-t_j+\Delta t_j}{\gamma_j}\right)^2\right]
$$

donde:

$$
\omega'_j=\omega_j\sqrt{1-\xi_j^2}
$$

y $\gamma_j$ controla la duración de la wavelet.

El parámetro de duración debe ser coherente con la duración del contenido del registro en esa frecuencia. Si la wavelet es demasiado larga, introduce energía artificial; si es demasiado corta, puede no modificar adecuadamente la ordenada espectral.

### 13.6 Wavelet mejorada de Al Atik-Abrahamson

La mejora principal es usar una función de ajuste que:

- preserve la no estacionariedad;
- integre a velocidad y desplazamiento cero;
- evite deriva sin necesidad de corrección de línea base posterior;
- permita calcular analíticamente la matriz $C$;
- sea estable y eficiente;
- permita ajuste a pseudo-aceleración.

La idea física: la wavelet debe agregar energía localizada en tiempo-frecuencia sin introducir offset neto en velocidad o desplazamiento.

### 13.7 Dynamic padding

Si el pico de respuesta para una frecuencia baja ocurre demasiado cerca del inicio del registro, la wavelet no tiene tiempo suficiente para comenzar desde cero e integrar a cero. Se agrega padding de ceros al inicio para desplazar el registro y evitar deriva.

### 13.8 Barridos por rangos de periodo

Para preservar mejor la no estacionariedad:

1. escalar linealmente primero;
2. ajustar periodos cortos y medios;
3. ajustar periodos largos;
4. repetir con menor relajación;
5. verificar deriva, duración y energía.

El ajuste de periodos largos suele ser el más delicado, porque pequeñas modificaciones pueden cambiar fuertemente desplazamientos y pulsos.

---

## 14. Algoritmos avanzados con wavelets

### 14.1 Por qué wavelets

La transformada de Fourier describe contenido frecuencial promedio, pero no indica cuándo aparece cada frecuencia. Los terremotos son no estacionarios. La transformada wavelet representa simultáneamente tiempo y escala/frecuencia.

### 14.2 Transformada wavelet continua (CWT)

La CWT de una señal $a(t)$:

$$
W(s,\tau)=\frac{1}{\sqrt{s}}
\int_{-\infty}^{\infty}
a(t)\psi^*\left(\frac{t-\tau}{s}\right)dt
$$

donde:

- $s$: escala;
- $\tau$: traslación temporal;
- $\psi$: wavelet madre;
- $*$: conjugado complejo.

La escala se relaciona con frecuencia. Para cada escala se obtiene una función de detalle temporal. El registro puede reconstruirse como suma de detalles:

$$
a(t)\approx \sum_{k=1}^{n}D_k(t)
$$

### 14.3 Ajuste CWT por factores de escala

Procedimiento conceptual:

1. seleccionar escalas $s_k$ asociadas a periodos $T_k$;
2. calcular CWT del registro semilla;
3. reconstruir detalles $D_k(t)$;
4. estimar contribución de cada detalle al espectro;
5. multiplicar detalles por factores $x_k$;
6. reconstruir:
   $$
   a_{mod}(t)=\sum_k x_kD_k(t)
   $$
7. calcular $S_a(T)$;
8. iterar hasta convergencia.

Función objetivo:

$$
J(\mathbf{x})=
\sum_{j=1}^{n_T}
w_j
\left[
\ln S_{a,mod}(T_j;\mathbf{x})
-\ln S_{a,tar}(T_j)
\right]^2
+
\lambda_E P_E+
\lambda_D P_D+
\lambda_F P_F
$$

donde:

- $P_E$: penalización por cambio de energía;
- $P_D$: penalización por cambio de duración;
- $P_F$: penalización por distorsión excesiva del espectro de Fourier o PSD.

### 14.4 Ajuste DWT por optimización

En DWT, la señal se descompone en coeficientes discretos:

$$
a(t) \rightarrow \{cA_L,cD_L,cD_{L-1},...,cD_1\}
$$

Se ajustan coeficientes mediante factores:

$$
cD_{\ell}^{mod}=x_{\ell}\odot cD_{\ell}
$$

o con factores dependientes del tiempo:

$$
cD_{\ell,k}^{mod}=x_{\ell,k}cD_{\ell,k}
$$

La función objetivo puede ser:

$$
J(\mathbf{x})=
\sum_{i=1}^{n_T}
\sum_{r=1}^{n_\xi}
w_{i,r}
\left[
\ln S_{a,mod}(T_i,\xi_r;\mathbf{x})
-\ln S_{a,tar}(T_i,\xi_r)
\right]^2
$$

con restricciones:

$$
|v_{mod}(t_{end})|<\epsilon_v
$$

$$
|d_{mod}(t_{end})|<\epsilon_d
$$

$$
\left|\frac{I_{A,mod}}{I_{A,seed}}-1\right|<\epsilon_{IA}
$$

$$
|D_{5-95,mod}-D_{5-95,seed}|<\epsilon_D
$$

### 14.5 Penalizaciones recomendadas

Para preservar realismo:

**Energía:**

$$
P_E=
\left[
\ln\left(\frac{I_{A,mod}}{I_{A,seed,s}}\right)
\right]^2
$$

donde $seed,s$ es el registro semilla ya escalado linealmente.

**Duración:**

$$
P_D=
\left[
\frac{D_{5-95,mod}-D_{5-95,seed,s}}
{D_{5-95,seed,s}}
\right]^2
$$

**Fourier:**

$$
P_F=
\sum_{k}
w_k
\left[
\ln\left(\frac{FAS_{mod}(f_k)}{FAS_{seed,s}(f_k)}\right)
\right]^2
$$

**Deriva:**

$$
P_{drift}=
\left(\frac{v_{end}}{PGV}\right)^2+
\left(\frac{d_{end}}{PGD}\right)^2
$$

### 14.6 Ventajas del enfoque wavelet avanzado

- modifica contenido frecuencial localizado en el tiempo;
- conserva mejor envolvente temporal;
- permite imponer restricciones energéticas;
- puede controlar simultáneamente PSA, FAS y PSD;
- permite ajuste por componentes o por RotD;
- es más defendible que modificar amplitudes de Fourier globalmente.

### 14.7 Riesgos

- sobreajuste espectral;
- pérdida de dispersión natural;
- modificación de pulsos;
- energía artificial en ventanas sin movimiento;
- convergencia numérica falsa;
- registros que cumplen espectro pero fallan física de velocidad/desplazamiento;
- uso de wavelets para corregir una mala selección de registro semilla.

---

## 15. Ensamblaje matemático completo para ajuste espectral con wavelets

### 15.1 Datos de entrada

Para cada registro $k$:

$$
\mathbf{a}_k=[a_1,a_2,\dots,a_n]^T,\quad \Delta t
$$

Espectro objetivo:

$$
\mathbf{S}_{tar}=[S_{tar}(T_1),...,S_{tar}(T_m)]^T
$$

Periodos:

$$
\mathbf{T}=[T_1,...,T_m]^T
$$

Amortiguamiento:

$$
\boldsymbol{\xi}=[\xi_1,...,\xi_q]^T
$$

Pesos:

$$
\mathbf{w}=[w_1,...,w_m]^T
$$

### 15.2 Paso 1: espectro actual

Calcular:

$$
S_{k}^{(r)}(T_i,\xi)=\mathcal{R}\left(a_k^{(r)}(t),T_i,\xi\right)
$$

donde $\mathcal{R}$ es el operador de respuesta SDOF.

### 15.3 Paso 2: error

$$
\eta_i^{(r)}=\ln S_{tar}(T_i)-\ln S_k^{(r)}(T_i)
$$

o, en forma lineal con polaridad:

$$
\Delta R_i^{(r)}=(Q_i-R_i)P_i
$$

### 15.4 Paso 3: funciones de ajuste

Definir wavelets:

$$
f_j(t)=\psi_j(t;t_j,\omega_j,\xi_j,\gamma_j)
$$

con $t_j$ igual o cercano al tiempo de pico de respuesta del oscilador para $T_j$.

### 15.5 Paso 4: matriz de sensibilidad

$$
C_{ij}=
\int_0^{t_i}f_j(\tau)h_i(t_i-\tau)d\tau
$$

En forma discreta:

$$
C_{ij}\approx
\sum_{q=1}^{n_i}f_j(t_q)h_i(t_i-t_q)\Delta t
$$

### 15.6 Paso 5: regularización

Resolver:

$$
\min_{\mathbf{b}}
\|\mathbf{W}^{1/2}(\mathbf{C}\mathbf{b}-\Delta\mathbf{R})\|_2^2+
\lambda\|\mathbf{L}\mathbf{b}\|_2^2
$$

Solución:

$$
\mathbf{b}=
(\mathbf{C}^T\mathbf{W}\mathbf{C}+\lambda\mathbf{L}^T\mathbf{L})^{-1}
\mathbf{C}^T\mathbf{W}\Delta\mathbf{R}
$$

Con SVD:

$$
\mathbf{C}=\mathbf{U}\boldsymbol{\Sigma}\mathbf{V}^T
$$

$$
\mathbf{b}=
\mathbf{V}\boldsymbol{\Sigma}_{reg}^{-1}\mathbf{U}^T\Delta\mathbf{R}
$$

donde se eliminan valores singulares pequeños.

### 15.7 Paso 6: actualización

$$
\Delta a^{(r)}(t)=\sum_{j=1}^{m}b_jf_j(t)
$$

$$
a^{(r+1)}(t)=a^{(r)}(t)+\lambda_r\Delta a^{(r)}(t)
$$

### 15.8 Paso 7: convergencia

Converge si:

$$
\max_i |\eta_i^{(r)}| \leq \eta_{tol}
$$

y además:

$$
|v_{end}| \leq v_{tol}
$$

$$
|d_{end}| \leq d_{tol}
$$

$$
\left|\frac{I_{A,mod}}{I_{A,seed,s}}-1\right| \leq I_{tol}
$$

$$
\left|\frac{D_{5-95,mod}}{D_{5-95,seed}}-1\right| \leq D_{tol}
$$

### 15.9 Criterio de rechazo

Rechazar el registro si:

- no converge en iteraciones razonables;
- requiere demasiadas wavelets;
- cambia significativamente el Husid plot;
- genera energía antes del arribo físico;
- distorsiona pulso de velocidad;
- produce desplazamiento residual artificial;
- cambia $I_A$, $D_{5-95}$, PGV o FAS fuera de límites;
- el ajuste es mayor que lo que un escalamiento lineal razonable justificaría.

---

## 16. Metodología práctica completa

### 16.1 Flujo general

1. Definir objetivo de análisis.
2. Definir espectro objetivo y ubicación del movimiento.
3. Definir rango de periodos.
4. Definir amortiguamiento.
5. Seleccionar base de datos de registros.
6. Filtrar candidatos por magnitud, distancia, mecanismo, sitio y calidad.
7. Calcular espectros e indicadores.
8. Seleccionar registros por forma espectral y metadata.
9. Aplicar escalamiento lineal inicial.
10. Verificar compatibilidad de media del conjunto.
11. Si no cumple, aplicar ajuste espectral con wavelets.
12. Verificar espectro individual y de conjunto.
13. Verificar parámetros físicos.
14. Documentar resultados.
15. Ejecutar análisis estructural/geotécnico.
16. Revisar sensibilidad.

### 16.2 Pseudocódigo

```text
entrada:
    registros candidatos
    espectro objetivo S_tar(T)
    rango T_min, T_max
    amortiguamiento xi
    pesos w(T)
    tolerancias espectrales y físicas

para cada registro:
    leer acelerograma
    convertir unidades
    verificar dt y duración
    aplicar o revisar corrección de línea base
    calcular PGA, PGV, PGD
    calcular Ia y D5-95
    calcular Sa(T, xi)

filtrar candidatos:
    metadata compatible
    rango de periodos confiable
    forma espectral razonable
    duración adecuada
    sin procesamiento dudoso

para cada registro seleccionado:
    calcular factor lineal s
    a_scaled = s * a
    calcular error espectral

si conjunto cumple:
    aceptar escalamiento lineal
si no:
    para cada registro:
        inicializar a_match = a_scaled
        repetir hasta convergencia:
            calcular Sa(a_match)
            calcular misfit
            ubicar tiempos de pico por periodo
            ensamblar matriz C de wavelets
            resolver amplitudes b con regularización
            actualizar a_match
            verificar deriva y estabilidad
        calcular indicadores físicos
        si cambios excesivos:
            rechazar y seleccionar otro registro

salida:
    registros escalados/ajustados
    factores de escala
    espectros comparativos
    métricas de error
    tabla de parámetros antes/después
    justificación de aceptación
```

---

## 17. Aplicación geotécnica: sitio, deconvolución y compatibilidad

### 17.1 Movimiento de afloramiento vs movimiento dentro del perfil

**Movimiento de afloramiento:** movimiento que se registraría en superficie libre de roca sin suelo sobreyacente.

**Movimiento within:** movimiento dentro de un medio, a cierta profundidad, donde existe interacción de ondas ascendentes y descendentes.

En modelos 1D/2D, el input requerido puede ser:

- outcrop rock motion;
- within rock motion;
- base compliant;
- base rígida;
- movimiento de superficie de control.

Usar el tipo incorrecto puede duplicar o reducir indebidamente la energía.

### 17.2 Deconvolución

Si el objetivo está en superficie, pero el modelo requiere entrada en base, se necesita deconvolución:

$$
a_{base} = \mathcal{D}(a_{surface}, G(z), \xi(z), V_s(z), \rho(z))
$$

donde $\mathcal{D}$ representa el operador inverso de propagación de ondas.

### 17.3 Escalamiento antes o después de respuesta de sitio

Depende del objetivo:

**Caso A: objetivo en roca.**  
Escalar o ajustar registros en roca y luego propagar por el perfil.

**Caso B: objetivo en superficie.**  
Puede requerirse iteración:

1. seleccionar registro en roca;
2. escalar input;
3. ejecutar respuesta de sitio;
4. comparar espectro superficial con objetivo;
5. ajustar input o seleccionar otro registro;
6. repetir.

**Caso C: diseño geotécnico no lineal.**  
No conviene ajustar excesivamente en superficie si el análisis de suelo no lineal cambiará el contenido frecuencial. Debe controlarse simultáneamente input y output.

### 17.4 Suelos no lineales y duración

En suelos no lineales, la duración y el número de ciclos importan tanto como $S_a$. Para licuefacción:

- preservar $D_{5-95}$;
- preservar Arias;
- preservar CAV;
- preservar número de ciclos equivalentes;
- seleccionar magnitudes compatibles;
- no usar registros corticales cortos para representar subducción larga solo porque coinciden espectralmente.

### 17.5 Taludes, presas y relaves

Para análisis de deformación permanente:

- PGV e intensidad de Arias suelen correlacionarse mejor que PGA;
- duración controla acumulación de deformación;
- espectro de desplazamiento en periodos largos es importante;
- filtros pasa-altas agresivos pueden eliminar desplazamientos relevantes;
- revisar desplazamiento residual del input.

### 17.6 Túneles y estructuras enterradas

Se debe controlar:

- deformación cortante del terreno;
- velocidad de partícula;
- longitud de onda relativa al diámetro;
- incoherencia espacial si aplica;
- componentes horizontales y verticales.

---

## 18. Criterios de aceptación y control de calidad

### 18.1 Criterios espectrales mínimos

Para cada registro:

$$
|e(T_j)|\leq e_{max}
$$

para todo $T_j\in[T_{\min},T_{\max}]$.

Para conjunto:

$$
\bar S_a(T_j)\geq c\,S_{tar}(T_j)
$$

donde $c$ puede ser 0.90 si el criterio normativo lo permite.

También controlar sobreelevación:

$$
\bar S_a(T_j)\leq c_{sup}S_{tar}(T_j)
$$

si se busca evitar demandas artificialmente altas.

### 18.2 Criterios físicos recomendados

Comparar antes y después del ajuste:

| Parámetro | Recomendación |
|---|---|
| PGA | justificar cambios grandes |
| PGV | mantener coherente con escenario |
| PGD | revisar sensibilidad a línea base |
| $I_A$ | controlar cambio relativo |
| $D_{5-95}$ | preservar para análisis cíclicos |
| CAV | revisar daño acumulado |
| FAS | evitar distorsión fuera de banda |
| Husid | conservar forma de acumulación de energía |
| velocidad residual | cercana a cero salvo desplazamiento permanente real |
| desplazamiento residual | cercana a cero salvo caso físico |
| pulso near-fault | preservar amplitud, periodo y fase |
| componentes horizontales | preservar coherencia y orientación |

### 18.3 Métricas de cambio

$$
r_{PGA}=\frac{PGA_{mod}}{PGA_{seed,s}}
$$

$$
r_{PGV}=\frac{PGV_{mod}}{PGV_{seed,s}}
$$

$$
r_{IA}=\frac{I_{A,mod}}{I_{A,seed,s}}
$$

$$
\Delta D=\frac{D_{5-95,mod}-D_{5-95,seed,s}}{D_{5-95,seed,s}}
$$

$$
\Delta FAS=
\sqrt{
\frac{1}{n_f}
\sum_k
\left[
\ln\left(\frac{FAS_{mod}(f_k)}{FAS_{seed,s}(f_k)}\right)
\right]^2
}
$$

### 18.4 Tolerancias físicas sugeridas

No son universales, pero sirven como punto de partida:

- $r_{IA}$: dentro de 0.5 a 2.0 si el ajuste no pretende cambiar energía; más estricto si se evalúa licuefacción.
- $\Delta D_{5-95}$: menor a 10%-20% para análisis sensibles a duración.
- $r_{PGV}$: revisar si cambia más de 30%-50%.
- FAS: evitar cambios grandes fuera del rango de periodos ajustado.
- deriva final: $v_{end}$ y $d_{end}$ pequeños respecto a PGV y PGD.

---

## 19. Errores comunes

1. **Escalar por PGA cuando la estructura es sensible a $S_a(T_1)$.**

2. **Usar un UHS como si fuera un terremoto real único.**

3. **Ajustar cada registro exactamente al UHS y eliminar toda dispersión.**

4. **No definir el rango de periodos antes del ajuste.**

5. **No revisar duración significativa ni Arias.**

6. **Aplicar filtros sin documentar periodo máximo confiable.**

7. **Usar registros de subducción y corticales indistintamente.**

8. **Modificar registros near-fault hasta destruir el pulso de velocidad.**

9. **Escalar componentes horizontales de forma independiente sin criterio.**

10. **Usar movimiento de superficie como input en base sin deconvolución.**

11. **Aceptar registros ajustados solo porque el espectro coincide.**

12. **No conservar trazabilidad: factor de escala, filtros, software, iteraciones, tolerancias.**

---

## 20. Reporte técnico mínimo

Un informe de escalamiento debe incluir:

### 20.1 Objetivo

- tipo de análisis;
- nivel de amenaza;
- espectro objetivo;
- ubicación del movimiento;
- normativa o criterio de proyecto.

### 20.2 Registros

Tabla con:

- evento;
- estación;
- componente;
- magnitud;
- distancia;
- mecanismo;
- $V_{S30}$;
- $\Delta t$;
- duración;
- PGA, PGV, $I_A$, $D_{5-95}$;
- factor de escala;
- método de ajuste.

### 20.3 Procesamiento

- fuente del registro;
- filtros;
- corrección de línea base;
- unidades;
- interpolación o re-muestreo;
- pads y tapers.

### 20.4 Compatibilidad espectral

- espectros individuales antes/después;
- media del conjunto;
- error máximo;
- error RMS;
- periodos de control;
- amortiguamiento.

### 20.5 Control físico

- comparación de aceleración, velocidad y desplazamiento;
- Arias y Husid;
- duración significativa;
- FAS/PSD;
- pulsos;
- residuos de velocidad/desplazamiento.

### 20.6 Archivos entregables

- acelerogramas finales;
- espectros calculados;
- scripts o configuración de software;
- tabla de trazabilidad;
- figuras;
- registro de versiones.

---

## 21. Ejemplo conceptual de cálculo

Supóngase:

- estructura con $T_1=1.0$ s;
- rango de ajuste: $0.2T_1$ a $2T_1$, es decir 0.2 a 2.0 s;
- amortiguamiento 5%;
- espectro objetivo $S_{tar}(T)$;
- registro semilla $a(t)$.

### 21.1 Escalamiento lineal logarítmico

Para periodos $T_j$:

$$
\ln s=
\frac{1}{n}\sum_j
\left[
\ln S_{tar}(T_j)-\ln S_{rec}(T_j)
\right]
$$

$$
a_s(t)=s\,a(t)
$$

### 21.2 Revisión de error

$$
e_j=\frac{sS_{rec}(T_j)}{S_{tar}(T_j)}-1
$$

Si:

$$
\max |e_j| \leq 0.10
$$

se puede aceptar si los demás registros hacen que la media del conjunto cumpla.

### 21.3 Si no cumple

Aplicar wavelets:

$$
a_m(t)=a_s(t)+\sum_jb_jf_j(t)
$$

Iterar hasta:

$$
\max_j\left|\ln\frac{S_m(T_j)}{S_{tar}(T_j)}\right|\leq \ln(1.05)
$$

y:

$$
\left|\frac{I_{A,m}}{I_{A,s}}-1\right|\leq 0.20
$$

$$
\left|\frac{D_{5-95,m}}{D_{5-95,s}}-1\right|\leq 0.10
$$

---

## 22. Recomendaciones por tipo de proyecto

### 22.1 Edificaciones convencionales

- usar al menos 7 pares si se reporta media;
- seleccionar por forma espectral y metadata;
- evitar ajuste excesivo;
- controlar rango de 0.2$T_1$ a 1.5-2.0$T_1$;
- usar componentes horizontales coherentes.

### 22.2 Edificios altos o aislados

- considerar periodos largos;
- usar espectro multi-periodo o sitio-específico;
- controlar $S_d$ y PGD;
- revisar filtros de periodo largo;
- considerar cuencas y directividad.

### 22.3 Presas, relaves y taludes

- seleccionar por magnitud y duración;
- preservar $I_A$, CAV y $D_{5-95}$;
- revisar PGV;
- evitar filtros que eliminen deformación permanente;
- usar análisis de sensibilidad con varios conjuntos.

### 22.4 Licuefacción

- no usar solo compatibilidad espectral;
- seleccionar magnitud representativa;
- controlar duración y ciclos;
- usar Arias, CAV y número equivalente de ciclos;
- revisar respuesta de sitio no lineal.

### 22.5 Instalaciones críticas

- usar espectros de sitio específicos;
- documentar procesamiento exhaustivamente;
- controlar PSD/FAS además de PSA;
- usar conjuntos amplios;
- mantener trazabilidad reproducible;
- aplicar criterios regulatorios específicos.

---

## 23. Software y herramientas

### 23.1 RSPMatch

Implementa ajuste espectral en dominio del tiempo mediante wavelets. Versiones modernas incorporan wavelet mejorada, padding dinámico, pseudo-aceleración y reducción de deriva.

### 23.2 SeismoMatch

Herramienta comercial que implementa algoritmos de Abrahamson/Hancock y Al Atik-Abrahamson, permite ajustar registros y conjuntos, definir tolerancias, revisar misfit, calcular parámetros de movimiento fuerte, Arias y duración.

### 23.3 REQPY / enfoques CWT

Herramientas en Python con metodologías CWT para compatibilidad PSA y, en versiones recientes, restricciones de FAS/PSD.

### 23.4 Scripts propios

Un script reproducible debe contener:

- lectura de registros;
- procesamiento;
- cálculo de espectros;
- cálculo de $I_A$, Husid y duración;
- escalamiento lineal;
- selección por error;
- ajuste wavelet;
- verificación física;
- exportación de resultados.

---

## 24. Lista de chequeo final

Antes de aceptar un conjunto de registros:

- [ ] El espectro objetivo está definido y documentado.
- [ ] La ubicación del movimiento está clara.
- [ ] El rango de periodos está justificado.
- [ ] Los registros tienen metadata compatible.
- [ ] Se revisó calidad y procesamiento.
- [ ] Se calcularon PGA, PGV, PGD.
- [ ] Se calculó intensidad de Arias.
- [ ] Se calculó duración significativa.
- [ ] Se calculó respuesta elástica.
- [ ] Si corresponde, se calculó respuesta inelástica.
- [ ] Los factores de escala son razonables.
- [ ] La media del conjunto cumple el criterio.
- [ ] Los registros individuales no están sobredistorsionados.
- [ ] Se verificó velocidad y desplazamiento.
- [ ] Se revisó FAS/PSD.
- [ ] Se preservaron pulsos si existen.
- [ ] Se documentó software, versión y parámetros.
- [ ] Se guardaron registros finales y trazabilidad.

---

## 25. Conclusiones técnicas

1. El escalamiento sísmico debe formularse como un problema de compatibilidad entre amenaza, sitio, estructura y respuesta esperada.

2. El escalamiento lineal es transparente y preserva la forma temporal, pero puede no lograr compatibilidad en un rango amplio de periodos.

3. El ajuste espectral con wavelets es potente porque modifica contenido tiempo-frecuencia de forma localizada, preservando mejor la no estacionariedad que métodos puramente frecuenciales.

4. La compatibilidad espectral no garantiza realismo físico. Siempre deben revisarse Arias, duración, PGV, desplazamiento, FAS/PSD, Husid y pulsos.

5. En geotecnia sísmica, la duración, energía y número de ciclos pueden ser tan importantes como $S_a(T)$.

6. Para near-fault, subducción, licuefacción, presas y relaves, la selección del registro semilla es más importante que forzar una coincidencia espectral exacta.

7. Un procedimiento defendible es reproducible, documentado, trazable y consciente de sus supuestos.

---

---

## 26. Anexo técnico ampliado: cómo funciona SeismoMatch y cómo realiza el ajuste/escalamiento sísmico

> **Propósito de este anexo.** Esta sección explica SeismoMatch como herramienta de *response-spectrum matching* en dominio del tiempo. No debe interpretarse como una reproducción del código fuente propietario de SeismoSoft, sino como una reconstrucción técnica del procedimiento a partir de la documentación oficial de SeismoMatch, la literatura base de RspMatch/RspMatch09 y los artículos de Abrahamson, Hancock et al. y Al Atik & Abrahamson. La idea práctica es que el usuario entienda qué calcula el programa, qué parámetros controla y qué verificaciones deben acompañar un registro ajustado.

### 26.1 Qué es SeismoMatch

SeismoMatch es un programa de SeismoSoft orientado a ajustar acelerogramas reales para que su espectro de respuesta, usualmente de pseudo-aceleración o aceleración espectral a un amortiguamiento definido, sea compatible con un espectro objetivo dentro de un rango de periodos especificado. Su función central no es generar movimientos artificiales desde cero, sino partir de registros reales y añadir correcciones localizadas en el tiempo mediante funciones tipo wavelet. La documentación oficial indica que el programa implementa algoritmos de wavelets asociados a Hancock et al. (2006), Abrahamson (1992) y Al Atik & Abrahamson (2010), y que también permite combinar varios registros ajustados para verificar un espectro medio compatible con una tolerancia predefinida.

La operación práctica sigue cuatro bloques:

1. cargar uno o varios acelerogramas;
2. definir el espectro objetivo;
3. seleccionar el algoritmo de ajuste, rango de periodos, escala inicial y tolerancia;
4. ejecutar el ajuste y verificar misfit, iteraciones, convergencia y parámetros de movimiento fuerte.

La documentación oficial de 2026 también indica que SeismoMatch calcula, entre otros, espectros elásticos/pseudo-espectros, espectros sobreamortiguados, PGA, PGV, PGD, RMS, intensidad de Arias, intensidad característica, CAV, SED, ASI, VSI, intensidad de Housner, periodo predominante y duración significativa.

### 26.2 Diferencia entre escalamiento lineal y ajuste espectral en SeismoMatch

SeismoMatch puede aplicar un **factor de escala** al registro de entrada, pero su contribución más importante es el **ajuste espectral**. Conviene distinguirlos:

**Escalamiento lineal**:

$$
a_s(t)=s\,a_0(t)
$$

donde $a_0(t)$ es el registro semilla y $s$ es un factor constante. Si la respuesta es lineal, todas las ordenadas espectrales escalan aproximadamente por el mismo factor:

$$
S_{a,s}(T,\xi)=s\,S_{a,0}(T,\xi)
$$

El factor óptimo de mínimos cuadrados, si se desea ajustar un conjunto de periodos $T_i$, puede estimarse como:

$$
s^*=\frac{\sum_{i=1}^{n} w_i S_{a,0}(T_i)S_{a,tar}(T_i)}{\sum_{i=1}^{n} w_i S_{a,0}^2(T_i)}
$$

Si se minimiza el error logarítmico, una forma robusta es:

$$
\ln s^*=\frac{\sum_{i=1}^{n}w_i\left[\ln S_{a,tar}(T_i)-\ln S_{a,0}(T_i)\right]}{\sum_{i=1}^{n}w_i}
$$

**Ajuste espectral con wavelets**:

$$
a_m(t)=a_0(t)+\delta a(t)
$$

$$
\delta a(t)=\sum_{j=1}^{N} b_j f_j(t)
$$

Aquí $f_j(t)$ son wavelets de ajuste asociadas a periodos/frecuencias del espectro, y $b_j$ son amplitudes calculadas iterativamente para reducir la diferencia entre el espectro del registro y el espectro objetivo. A diferencia del escalamiento lineal, el ajuste espectral cambia selectivamente el contenido de frecuencia y tiempo del registro.

### 26.3 Datos que SeismoMatch necesita para trabajar correctamente

Para usar SeismoMatch con rigor deben estar definidos, como mínimo:

| Categoría | Datos requeridos | Comentario técnico |
|---|---|---|
| Acelerograma | aceleración, $\Delta t$, número de puntos, unidades, componente, evento, estación | El programa puede leer distintos formatos; para archivos de texto, el usuario define líneas a leer, columna de aceleración, paso de tiempo y factor de escala. |
| Unidades | $g$, $m/s^2$, $cm/s^2$, etc. | El factor de escala de lectura puede usarse para convertir, por ejemplo, de $g$ a $m/s^2$. |
| Espectro objetivo | $S_{a,tar}(T,\xi)$, amortiguamiento, rango de periodos | Puede ser normativo, definido por el usuario, o el espectro de otro acelerograma. |
| Parámetros de matching | algoritmo, tolerancia, rango $[T_{min},T_{max}]$, iteraciones, SVD, número de wavelets | Controlan convergencia y grado de modificación de la señal. |
| Control de calidad | PGA, PGV, PGD, $I_A$, duración, CAV, FAS/PSD, deriva residual | La coincidencia espectral no basta: debe preservarse plausibilidad física. |

El cargado de registros en SeismoMatch admite archivos con un valor por línea, pares tiempo-aceleración, múltiples valores por línea, ESM, SMC, PEER NGA y SHAKE `.eq`. Para registros de texto, el usuario debe definir línea inicial, línea final, paso de tiempo y factor de escala; para archivos largos, SeismoSoft advierte que un número excesivo de puntos puede aumentar memoria y tiempo de análisis, por lo que puede ser razonable recortar tramos de movimiento despreciable o revisar el $\Delta t$.

### 26.4 Definición del espectro objetivo en SeismoMatch

SeismoMatch permite tres vías principales para definir el objetivo:

1. **Espectro normativo**. El usuario selecciona una norma y define parámetros sísmicos y de sitio. La documentación oficial lista, entre otros, Eurocode 8, ASCE 41, ASCE 7, UBC 1997, NBCC 2015, NTE E.030, NCh 433, NSR-10, NEC 2014, NZS 1170.5, normas mexicanas y otras.
2. **Espectro de un acelerograma cargado**. Útil para compatibilizar un conjunto contra un registro de referencia o para pruebas metodológicas.
3. **Espectro desde archivo**. Usualmente dos columnas: periodo $T$ y aceleración espectral. El programa añade una ordenada en $T=0$ si no está explícita.

Matemáticamente, el objetivo se discretiza como un vector de ordenadas:

$$
\mathbf{Q}=\left[Q_1,Q_2,\ldots,Q_N\right]^T
$$

con:

$$
Q_i=S_{a,tar}(T_i,\xi_i) \quad \text{o} \quad Q_i=PSA_{tar}(T_i,\xi_i)
$$

El rango de periodos debe escogerse por ingeniería, no por conveniencia gráfica. Para análisis estructural, debe cubrir modos superiores, periodo fundamental y elongación por daño. Para análisis geotécnico, debe cubrir los periodos que controlan deformación cortante, desplazamiento permanente, respuesta de sitio, licuefacción o interacción suelo-estructura. SeismoSoft advierte que un rango exageradamente amplio puede degradar la no estacionariedad del registro o impedir la convergencia.

### 26.5 Respuesta elástica que SeismoMatch calcula

La base del espectro de respuesta es el oscilador lineal de un grado de libertad sometido a aceleración basal $a_g(t)$:

$$
m\ddot u(t)+c\dot u(t)+ku(t)=-m a_g(t)
$$

con:

$$
\omega_n=\sqrt{\frac{k}{m}}, \qquad T=\frac{2\pi}{\omega_n}, \qquad c=2\xi m\omega_n
$$

La respuesta relativa $u(t)$, velocidad relativa $\dot u(t)$, aceleración relativa $\ddot u(t)$ y aceleración absoluta $\ddot u(t)+a_g(t)$ permiten definir:

$$
S_d(T,\xi)=\max_t |u(t)|
$$

$$
S_v(T,\xi)=\max_t |\dot u(t)|
$$

$$
S_a(T,\xi)=\max_t |\ddot u(t)+a_g(t)|
$$

Los pseudo-espectros se obtienen desde el desplazamiento máximo:

$$
PSV(T,\xi)=\omega_n S_d(T,\xi)
$$

$$
PSA(T,\xi)=\omega_n^2 S_d(T,\xi)
$$

La documentación de SeismoMatch indica que los espectros elásticos se calculan mediante integración temporal de una serie de osciladores SDOF, y que el esquema empleado es Newmark. El esquema de Newmark usa parámetros $\beta_N$ y $\gamma_N$; la condición de estabilidad incondicional es:

$$
\beta_N \geq \frac{1}{4}(\gamma_N+0.5)^2
$$

Con la regla trapezoidal no disipativa:

$$
\beta_N=0.25, \qquad \gamma_N=0.5
$$

SeismoMatch usa por defecto $\beta_N=0.25$, $\gamma_N=0.5$ y controla la precisión imponiendo un máximo $\Delta t/T$, por defecto alrededor de 0.02 en su documentación de espectros. Si el paso del acelerograma es demasiado grande respecto al periodo del oscilador, el programa interpola localmente la señal para cumplir ese límite.

### 26.6 Medida de error espectral o *misfit*

Para cada periodo/frecuencia $i$, el registro actual tiene una respuesta $R_i$, y el objetivo es $Q_i$. En el algoritmo base de Lilhanand-Tseng/Abrahamson, la diferencia se define con la polaridad del pico de respuesta:

$$
\Delta R_i=(Q_i-R_i)P_i
$$

$P_i=+1$ si el pico de respuesta del oscilador ocurre con signo positivo, y $P_i=-1$ si ocurre con signo negativo. Esta polaridad importa porque el ajuste se aplica en el instante del pico de respuesta $t_i$, y el algoritmo busca añadir una corrección cuya respuesta en ese instante tenga el signo correcto.

En control de aceptación ingenieril suele reportarse también el error relativo:

$$
e_i=\frac{S_{a,m}(T_i)-S_{a,tar}(T_i)}{S_{a,tar}(T_i)}
$$

Error máximo:

$$
e_{max}=\max_{T_i\in[T_{min},T_{max}]} |e_i|
$$

Error medio absoluto:

$$
e_{mean}=\frac{1}{N}\sum_{i=1}^{N}|e_i|
$$

Error logarítmico:

$$
e_{\ln,i}=\ln S_{a,m}(T_i)-\ln S_{a,tar}(T_i)
$$

$$
RMSE_{\ln}=\sqrt{\frac{1}{N}\sum_{i=1}^{N}e_{\ln,i}^2}
$$

SeismoMatch muestra resultado convergido/no convergido, misfit promedio, misfit máximo y número de iteraciones. La **mismatch tolerance** controla la convergencia por misfit máximo. En RSPMatch09 se menciona típicamente $Tol=0.05$, equivalente a 5% de desviación máxima respecto al objetivo; en SeismoMatch el usuario define la tolerancia según el proyecto.

### 26.7 Ensamblaje matricial del ajuste en dominio del tiempo

La idea central es construir una corrección de aceleración como combinación lineal de wavelets:

$$
\delta a(t)=\sum_{j=1}^{N}b_j f_j(t)
$$

donde:

- $f_j(t)$ es una función de ajuste asociada al punto espectral $j$;
- $b_j$ es la amplitud desconocida de esa wavelet;
- $N$ es el número de puntos espectrales a ajustar en el grupo o pase activo.

La respuesta del oscilador $i$ en el tiempo de pico $t_i$ causada por $\delta a(t)$ es:

$$
\delta R_i=\int_{0}^{\infty}\delta a(\tau)h_i(t_i-\tau)d\tau
$$

Sustituyendo la combinación de wavelets:

$$
\delta R_i=\sum_{j=1}^{N} b_j\int_{0}^{\infty} f_j(\tau)h_i(t_i-\tau)d\tau
$$

Se define el coeficiente de sensibilidad:

$$
C_{ij}=\int_{0}^{t_i} f_j(\tau)h_i(t_i-\tau)d\tau
$$

por lo tanto:

$$
\delta R_i=\sum_{j=1}^{N} C_{ij}b_j
$$

En forma matricial:

$$
\Delta\mathbf{R}=\mathbf{C}\mathbf{b}
$$

$$
\mathbf{b}=\mathbf{C}^{-1}\Delta\mathbf{R}
$$

En la práctica no se invierte $\mathbf{C}$ de forma ingenua. La matriz puede estar mal condicionada porque wavelets cercanas en periodo producen respuestas correlacionadas. Por ello se emplea descomposición en valores singulares:

$$
\mathbf{C}=\mathbf{U}\mathbf{\Sigma}\mathbf{V}^T
$$

$$
\mathbf{b}=\mathbf{V}\mathbf{\Sigma}^{+}\mathbf{U}^T\Delta\mathbf{R}
$$

con truncamiento de valores singulares pequeños:

$$
\sigma_k < \sigma_{max}\,\varepsilon_{eig} \quad \Rightarrow \quad \sigma_k^{+}=0
$$

En SeismoMatch, el parámetro **Min Eigen Value** controla este umbral normalizado; la documentación recomienda $10^{-4}$. Un valor menor acelera la convergencia, pero puede volverla menos estable.

La actualización iterativa es:

$$
a^{(r+1)}(t)=a^{(r)}(t)+\lambda\,\delta a^{(r)}(t)
$$

$\lambda$ es un parámetro de relajación o amortiguamiento numérico. En RSPMatch se denomina a veces $\Gamma$ y suele tomarse cercano a 1 cuando el sistema converge bien. Si hay oscilación o divergencia, puede reducirse la amplitud de la corrección, aumentar subiteraciones o usar wavelets adicionales.

### 26.8 Función impulso del oscilador

La literatura de Al Atik & Abrahamson presenta la función de respuesta impulsiva de aceleración para un oscilador SDOF con frecuencia circular $\omega_i$ y amortiguamiento $\xi_i$:

$$
\omega_i^d=\omega_i\sqrt{1-\xi_i^2}
$$

$$
h_i(t)= -\frac{\omega_i}{\sqrt{1-\xi_i^2}}\,e^{-\omega_i\xi_i t}
\left[(2\xi_i^2-1)\sin(\omega_i^d t)-2\xi_i\sqrt{1-\xi_i^2}\cos(\omega_i^d t)\right]
$$

con:

$$
h_i(t)=0 \quad \text{para}\quad t<0
$$

Para ajuste a pseudo-aceleración, el método mejorado usa la respuesta impulsiva de pseudo-aceleración:

$$
h_i^{PSA}(t)= -\frac{\omega_i}{\sqrt{1-\xi_i^2}}\,e^{-\omega_i\xi_i t}\sin(\omega_i^d t)
$$

El objetivo de usar $h_i(t)$ o $h_i^{PSA}(t)$ es que la wavelet añadida produzca una corrección precisa en la respuesta espectral del oscilador exactamente donde el registro actual tiene su máximo.

### 26.9 Los dos algoritmos de SeismoMatch

La documentación de SeismoMatch indica que el usuario puede escoger entre dos algoritmos wavelet:

1. **Hancock et al. (2006)**, relacionado con la mejora de RspMatch y el uso de wavelets corregidas para reducir deriva. Esta formulación puede requerir corrección de línea base para eliminar deriva en velocidad y desplazamiento, y utiliza integración numérica y wavelets adicionales para mejorar convergencia.
2. **Al Atik & Abrahamson (2010)**, que introduce una función de ajuste de coseno taperizado mejorada, con taper gaussiano, diseñada para no introducir deriva residual en velocidad y desplazamiento y permitir una solución más estable y eficiente.

La segunda formulación es la que se asocia a la versión mejorada de RSPMatch09 y al llamado *improved tapered cosine wavelet*.

### 26.10 Wavelet de Lilhanand-Tseng: impulso invertido

La wavelet original puede formularse como la respuesta impulsiva en orden temporal inverso:

$$
f_j(t)=h_j(t_j-t)
$$

Esta forma tiene ventajas numéricas: fuerza el máximo de respuesta cerca de $t_j$ y puede producir matrices de sensibilidad estables. Sin embargo, puede tener rasgos poco realistas: aparece antes del tiempo de pico, termina abruptamente y puede afectar la no estacionariedad del movimiento, especialmente en periodos largos. Por eso no es la opción preferente en aplicaciones modernas de SeismoMatch/RSPMatch.

### 26.11 Wavelet de coseno taperizado de Abrahamson/Hancock

Una función de ajuste usada en la evolución de RspMatch es el coseno taperizado:

$$
f_j(t)=\cos\left[\omega_j^d(t-t_j+\Delta t_j)\right]\exp\left[-\left|t-t_j+\Delta t_j\right|\alpha_j\right]
$$

El desfase $\Delta t_j$ se escoge para que la wavelet esté aproximadamente en fase con el pico de respuesta del oscilador:

$$
\Delta t_j=\frac{\tan^{-1}\left(\frac{\sqrt{1-\xi_j^2}}{\xi_j}\right)}{\omega_j^d}
$$

El parámetro $\alpha_j$ controla la duración de la wavelet y depende de la frecuencia. Un modelo trilineal usado en la literatura es:

$$
\alpha(f)=a_1 f \qquad f<f_1
$$

$$
\alpha(f)=\left[a_1+(a_2-a_1)\frac{f-f_1}{f_2-f_1}\right]f \qquad f_1\leq f\leq f_2
$$

$$
\alpha(f)=a_2 f \qquad f>f_2
$$

En SeismoMatch se listan los parámetros $a_1,a_2,f_1,f_2$ y se recomiendan valores $1.25,0.25,1.0,4.0$, respectivamente. Cambiarlos sin justificación puede alterar la dependencia frecuencial de la función de ajuste y generar misfits altos o modificaciones no realistas.

Esta wavelet preserva mejor el carácter no estacionario que la wavelet de impulso invertido, pero puede introducir deriva en velocidad y desplazamiento porque su integral y doble integral no necesariamente se anulan:

$$
\int_{0}^{T_{rec}} f_j(t)\,dt \neq 0
$$

$$
\int_{0}^{T_{rec}}\int_{0}^{t} f_j(\tau)\,d\tau\,dt \neq 0
$$

Por eso el enfoque Hancock/RspMatch2005 puede requerir corrección de línea base después de las iteraciones.

### 26.12 Wavelet mejorada de Al Atik & Abrahamson

Al Atik & Abrahamson proponen una función de coseno con taper gaussiano:

$$
f_j(t)=\cos\left[\omega_j^d(t-t_j+\Delta t_j)\right]
\exp\left[-\left(\frac{t-t_j+\Delta t_j}{\sigma_j}\right)^2\right]
$$

El parámetro de duración se define como función de frecuencia:

$$
\sigma(f)=1.178f^{-0.93}
$$

Su ventaja es que el taper es suave y continuo, y la función se diseña para que sus contribuciones a velocidad y desplazamiento final sean nulas o prácticamente nulas cuando la wavelet completa cabe dentro del registro:

$$
\Delta v_j(T_{rec})=\int_{0}^{T_{rec}} f_j(t)\,dt \approx 0
$$

$$
\Delta d_j(T_{rec})=\int_{0}^{T_{rec}}\int_{0}^{t} f_j(\tau)\,d\tau\,dt \approx 0
$$

Por esto, el algoritmo de Al Atik & Abrahamson evita la deriva de velocidad/desplazamiento sin incorporar una corrección de línea base dentro de la forma funcional. Además, permite calcular de forma analítica elementos de la matriz $\mathbf{C}$, lo que mejora estabilidad y eficiencia.

### 26.13 Dynamic padding

Un problema aparece cuando el tiempo de pico $t_j$ del oscilador, especialmente a baja frecuencia, ocurre demasiado cerca del inicio del registro. En ese caso, la wavelet gaussiana no tiene espacio suficiente antes de $t_j$ para iniciar desde cero; la integral no se cancela y puede aparecer deriva.

El algoritmo mejorado resuelve esto mediante **dynamic padding**: añade ceros al inicio de la aceleración para desplazar efectivamente el tiempo de pico y permitir que la wavelet completa se desarrolle:

$$
a_p(t)=
\begin{cases}
0, & 0\leq t<t_{pad}\\
a(t-t_{pad}), & t\geq t_{pad}
\end{cases}
$$

El padding no añade energía sísmica real, pero modifica el origen temporal del registro. Después del ajuste, el usuario debe revisar que el registro final conserve inicio, fase fuerte y duración físicamente interpretables. La documentación de SeismoMatch permite activar o desactivar este padding dinámico.

### 26.14 Parámetros de ajuste en SeismoMatch y su interpretación técnica

| Parámetro | Significado | Efecto práctico | Riesgo si se usa mal |
|---|---|---|---|
| **Mismatch tolerance** | Tolerancia máxima de error espectral | Controla criterio de convergencia | Tolerancia excesivamente baja puede forzar modificaciones artificiales. |
| **Max iterations** | Iteraciones máximas | Permite convergencia gradual | Muchas iteraciones pueden destruir no estacionariedad. |
| **Scale factor** | Factor de escala inicial/lectura | Convierte unidades o aproxima amplitud inicial | Confundir unidades produce demandas erróneas. |
| **Min Eigen Value** | Umbral SVD normalizado | Regulariza inversión de $\mathbf{C}$ | Muy bajo: inestabilidad; muy alto: convergencia lenta o pobre. |
| **Max number of waves** | Número máximo de wavelets del modelo | Controla flexibilidad del ajuste | Muy bajo: mal matching; muy alto: sobreajuste. |
| **Number additional waves** | Subiteraciones/wavelets adicionales | Evita divergencia o reduce amplitud | Puede ocultar que el registro semilla es inadecuado. |
| **Off diagonal reduction** | Reducción de términos fuera de diagonal de $\mathbf{C}$ | Mejora convergencia al desacoplar periodos | Valores demasiado bajos pueden corromper velocidad/desplazamiento. |
| **Group size** | Número de puntos espectrales por subgrupo | Divide el problema en bloques | Grupos mal elegidos pueden inducir inconsistencias entre rangos. |
| **Min/Max period** | Intervalo de ajuste | Define donde se exige compatibilidad | Rango demasiado amplio altera el registro; rango estrecho deja demandas fuera de control. |
| **a1,a2,f1,f2** | Parámetros del taper dependiente de frecuencia | Controlan duración de wavelets tipo coseno | Cambios no calibrados alteran dependencia frecuencial. |
| **Target PGA / Adjustment frequency** | Control de ajuste de PGA | Ajusta ordenada $T=0$ o periodo muy corto | Puede sobredominar el matching de periodos estructurales. |
| **Damping** | Amortiguamiento del espectro objetivo | Usualmente 5%, pero puede variar | Comparar registros a distinto $\xi$ invalida el matching. |
| **Dynamic padding** | Zeros antes del registro | Evita drift en wavelets tempranas | Cambia origen temporal; debe documentarse. |

La documentación oficial de SeismoMatch menciona explícitamente valores recomendados como $10^{-4}$ para **Min Eigen Value**, 10 para **Max number of waves**, 20 para **Number additional waves**, 0.7 para **Off diagonal reduction** y 250 para **Group size**. Estos valores no son universales; deben considerarse puntos de partida.

### 26.15 Proceso iterativo completo de SeismoMatch, expresado como algoritmo

Un esquema conceptual compatible con el procedimiento descrito por la literatura es:

```text
Entrada:
    acelerograma a0(t)
    dt, unidades
    espectro objetivo Q_i = Sa_tar(T_i, xi)
    rango [Tmin, Tmax]
    tolerancia e_tol
    algoritmo wavelet
    parámetros SVD / iteración / grupos

Preproceso:
    convertir unidades
    integrar para v(t), d(t)
    calcular espectro inicial R_i
    aplicar escala inicial si corresponde

Para iteración r = 0 ... maxIter:
    1. Calcular espectro actual R_i^(r)
    2. Calcular error relativo e_i y misfit ΔR_i
    3. Si max |e_i| <= e_tol: detener; registro convergido
    4. Para cada punto espectral i:
           localizar tiempo de pico t_i y polaridad P_i
    5. Construir wavelets f_j(t) según algoritmo elegido
    6. Ensamblar C_ij = ∫ f_j(τ) h_i(t_i - τ) dτ
    7. Regularizar/invertir C por SVD
    8. Resolver b = C^+ ΔR
    9. Construir δa(t)=Σ b_j f_j(t)
   10. Actualizar a^(r+1)(t)=a^(r)(t)+λ δa(t)
   11. Revisar drift, PGA, PGV, PGD, Arias y duración

Salida:
    registro ajustado a_m(t)
    espectro ajustado
    misfit máximo y promedio
    iteraciones usadas
    parámetros de movimiento fuerte
    reporte de aceptación/rechazo
```

### 26.16 Cómo interpreta SeismoMatch la convergencia

La convergencia no significa que el registro sea automáticamente adecuado. Significa que dentro del rango de periodos solicitado el misfit cumple la tolerancia. La evaluación debe cubrir:

$$
\max |e_i| \leq e_{tol}
$$

pero también:

$$
|PGA_m/PGA_0|,\ |PGV_m/PGV_0|,\ |PGD_m/PGD_0|
$$

$$
I_{A,m}/I_{A,0},\quad D_{5-95,m}/D_{5-95,0},\quad CAV_m/CAV_0
$$

$$
\max |v_m(T_{rec})|\approx 0,\qquad \max |d_m(T_{rec})|\approx 0
$$

Si el registro necesita muchas iteraciones para converger, SeismoSoft advierte que sus características no estacionarias probablemente se están perdiendo, por lo que conviene elegir otro registro semilla.

### 26.17 Parámetros de movimiento fuerte que deben revisarse después del matching

SeismoMatch calcula varios parámetros de control. Las ecuaciones base son:

**PGA, PGV, PGD**:

$$
PGA=\max_t |a(t)|
$$

$$
PGV=\max_t |v(t)|, \qquad v(t)=\int_0^t a(\tau)d\tau
$$

$$
PGD=\max_t |d(t)|, \qquad d(t)=\int_0^t v(\tau)d\tau
$$

**RMS**:

$$
a_{rms}=\sqrt{\frac{1}{T_d}\int_{t_1}^{t_2}a^2(t)dt}
$$

análogamente:

$$
v_{rms}=\sqrt{\frac{1}{T_d}\int_{t_1}^{t_2}v^2(t)dt}, \qquad d_{rms}=\sqrt{\frac{1}{T_d}\int_{t_1}^{t_2}d^2(t)dt}
$$

**Intensidad de Arias**:

$$
I_A=\frac{\pi}{2g}\int_0^{T_{rec}} a^2(t)dt
$$

**Intensidad de Arias normalizada**:

$$
I_A^N(t)=\frac{\int_0^t a^2(\tau)d\tau}{\int_0^{T_{rec}}a^2(\tau)d\tau}
$$

**Duración significativa**:

$$
D_{5-95}=t_{95}-t_{5}
$$

con:

$$
I_A^N(t_5)=0.05, \qquad I_A^N(t_{95})=0.95
$$

**CAV**:

$$
CAV=\int_0^{T_{rec}}|a(t)|dt
$$

**SED**:

$$
SED=\int_0^{T_{rec}}v^2(t)dt
$$

**Intensidad característica**:

$$
I_c=a_{rms}^{3/2}D^{1/2}
$$

usualmente con $D$ como una duración significativa o duración efectiva definida por el criterio del proyecto.

**ASI y VSI**:

$$
ASI=\int_{0.1}^{0.5}S_a(T,5\%)\,dT
$$

$$
VSI=\int_{0.1}^{2.5}S_v(T,5\%)\,dT
$$

**Intensidad de Housner**:

$$
HI=\int_{0.1}^{2.5}PSV(T,5\%)\,dT
$$

**Periodo predominante según SeismoMatch**:

$$
T_p=\operatorname*{arg\,max}_{T}\,S_a(T,5\%)
$$

Estas métricas sirven para detectar sobreajuste. Por ejemplo, un registro puede cumplir $S_a(T)$ y aun así tener incremento no razonable de $I_A$, pérdida de duración, CAV excesivo o desplazamiento residual artificial.

### 26.18 Respuesta inelástica: lo que SeismoMatch no controla directamente

SeismoMatch ajusta registros contra espectros elásticos o pseudo-elásticos. El usuario debe verificar si el ajuste introduce sesgos en análisis no lineales. Para un SDOF no lineal:

$$
m\ddot u(t)+c\dot u(t)+f_s(u,\dot u)=-m a_g(t)
$$

Para un modelo elastoplástico bilineal:

$$
f_s(u)=
\begin{cases}
ku, & |u|\leq u_y\\
\operatorname{sgn}(u)F_y+\alpha k(u-\operatorname{sgn}(u)u_y), & |u|>u_y
\end{cases}
$$

con:

$$
F_y=ku_y
$$

La ductilidad de desplazamiento es:

$$
\mu=\frac{u_{max}}{u_y}
$$

El factor de reducción por ductilidad puede expresarse como:

$$
R_\mu=\frac{S_{a,e}(T,\xi)}{S_{a,y}(T,\xi,\mu,\alpha)}
$$

El problema es que dos registros con el mismo espectro elástico pueden generar demandas inelásticas diferentes por duración, secuencia de pulsos, contenido energético y no estacionariedad. Por eso, para diseño basado en desempeño, el reporte no debe limitarse a espectros elásticos ajustados; debe incluir controles de duración, energía, pulsos y análisis de sensibilidad de respuesta inelástica.

### 26.19 Matching individual versus matching de espectro medio

SeismoMatch permite ajustar registros individuales y también combinar registros ajustados para verificar el espectro medio. Para un conjunto de $M$ registros:

$$
\overline{S}_a(T_i)=\frac{1}{M}\sum_{k=1}^{M}S_{a,k}(T_i)
$$

El error medio del conjunto es:

$$
\bar e_i=\frac{\overline{S}_a(T_i)-S_{a,tar}(T_i)}{S_{a,tar}(T_i)}
$$

Criterio típico:

$$
\max_{T_i\in[T_{min},T_{max}]} |\bar e_i|\leq e_{tol,mean}
$$

y, si se exige control individual:

$$
\max_i |e_{i,k}|\leq e_{tol,ind}\quad \forall k
$$

La decisión depende del marco normativo. En algunos enfoques se controla principalmente el promedio de la suite; en otros, registros individuales no deben caer por debajo de cierto porcentaje del espectro objetivo.

### 26.20 Criterios de aceptación recomendados al usar SeismoMatch

Para un informe técnico robusto, usar al menos estos criterios:

1. **Compatibilidad espectral**: reportar $e_{max}$, $e_{mean}$, $RMSE_{\ln}$ dentro del rango $[T_{min},T_{max}]$.
2. **Rango de matching justificado**: documentar por qué se eligió el rango. Para estructuras, incluir modos superiores y elongación; para geotecnia, incluir respuesta de sitio y periodos de deformación relevantes.
3. **Control de deriva**: revisar velocidad y desplazamiento residual. No aceptar registros con deriva artificial aunque el espectro coincida.
4. **Control de no estacionariedad**: comparar envolvente temporal, $I_A^N(t)$, $D_{5-95}$, FAS o espectrograma antes/después.
5. **Control de intensidad**: comparar PGA, PGV, PGD, CAV, SED e intensidad de Arias.
6. **Control de plausibilidad sismológica**: evitar registros cuyo contenido temporal ya no parezca consistente con el evento semilla.
7. **Control de número de iteraciones**: muchas iteraciones o grandes correcciones indican mala selección del registro semilla.
8. **Control de componentes**: si se usan dos horizontales, verificar coherencia entre componentes, orientación, RotD50/RotD100 o criterio normativo aplicable.
9. **Control geotécnico**: si el movimiento será deconvolucionado o propagado por un perfil de suelo, no ajustar en superficie y luego usarlo como roca sin transformar correctamente el movimiento.

### 26.21 Selección del registro semilla antes de usar SeismoMatch

El algoritmo de matching no reemplaza una selección sísmica adecuada. La literatura recomienda que los registros semilla representen razonablemente magnitud, distancia, tipo de falla, directividad y condiciones de sitio del escenario de amenaza. Una ventana práctica puede ser:

$$
|M_w-M_{esc}|\leq 0.5
$$

$$
|R-R_{esc}|\leq 10\text{ km} \quad \text{en campo cercano}
$$

$$
|R-R_{esc}|\leq 20\text{ km} \quad \text{en distancias mayores}
$$

Estas ventanas no son reglas absolutas; son criterios de partida. En geotecnia, además, revisar duración y energía es crítico, especialmente para licuefacción, acumulación de deformaciones y estabilidad de taludes o presas.

### 26.22 Uso de SeismoMatch en análisis geotécnico sísmico

Para ingeniería geotécnica, el punto crítico es **dónde** debe ser compatible el movimiento:

- Si el análisis de respuesta de sitio se hará desde roca aflorante, el registro objetivo debe representar roca aflorante.
- Si se aplica en base de columna 1D como movimiento dentro del medio (*within motion*), debe deconvolucionarse el movimiento de superficie o transformar adecuadamente el registro.
- Si se evalúa licuefacción, no basta ajustar $S_a$; deben revisarse duración, CAV, número equivalente de ciclos e intensidad de Arias.
- Si se evalúa desplazamiento permanente, son esenciales PGV, espectro de desplazamiento, duración y pulsos.
- Si hay no linealidad de suelo, un matching excesivo en superficie puede no conservar la señal apropiada en profundidad.

La cadena correcta es:

$$
\text{amenaza} \rightarrow \text{espectro objetivo} \rightarrow \text{selección de registros} \rightarrow \text{definición del punto de control} \rightarrow \text{matching/escala} \rightarrow \text{propagación/deconvolución} \rightarrow \text{verificación en el punto de demanda}
$$

No debe confundirse un registro compatible en superficie con un registro compatible en roca o en base de modelo geotécnico.

### 26.23 Problemas frecuentes al usar SeismoMatch

**Problema 1: se fuerza un registro inadecuado.** Si el registro semilla está muy lejos del objetivo, el algoritmo puede converger pero destruir duración, energía o forma temporal.

**Problema 2: rango de periodos demasiado amplio.** Exigir matching desde periodos muy cortos hasta muy largos puede hacer que el algoritmo añada correcciones incompatibles entre sí.

**Problema 3: tolerancia demasiado estricta.** Buscar error casi cero puede producir sobreajuste sin beneficio real para la respuesta estructural o geotécnica.

**Problema 4: no revisar desplazamientos.** Un espectro compatible con desplazamiento residual alto es físicamente sospechoso.

**Problema 5: ajustar cada componente horizontal de manera independiente sin revisar orientación.** Esto puede alterar la relación entre componentes, directividad y medidas RotD.

**Problema 6: usar UHS como si fuera un registro real.** Un espectro uniforme de amenaza ya incorpora variabilidad periodo por periodo; ajustar cada registro exactamente al UHS puede eliminar la variabilidad de la suite. SeismoSoft cita la advertencia de Jayaram et al. sobre no añadir varianza alrededor de un UHS cuando este ya incorpora variabilidad.

### 26.24 Plantilla de reporte específico para SeismoMatch

```text
Software:
    SeismoMatch versión:
    Sistema de unidades:
    Algoritmo usado:
        Hancock et al. (2006): sí/no
        Al Atik & Abrahamson (2010): sí/no

Registro semilla:
    Evento:
    Estación:
    Componente:
    Mw:
    Distancia:
    Mecanismo:
    Vs30 / clase de sitio:
    dt:
    Duración:
    Unidades originales:
    Procesamiento previo:

Espectro objetivo:
    Tipo: normativo / UHS / CMS / usuario / registro
    Amortiguamiento:
    Rango Tmin-Tmax:
    Número de puntos espectrales:
    Norma o fuente:

Parámetros de matching:
    Tolerancia:
    Iteraciones máximas:
    Factor de escala:
    Min Eigen Value:
    Max number of waves:
    Additional waves:
    Off diagonal reduction:
    Group size:
    Dynamic padding:
    a1, a2, f1, f2:

Resultados:
    Convergió: sí/no
    Iteraciones usadas:
    Misfit máximo:
    Misfit promedio:
    PGA original / ajustado:
    PGV original / ajustado:
    PGD original / ajustado:
    Arias original / ajustado:
    D5-95 original / ajustado:
    CAV original / ajustado:
    Velocidad residual:
    Desplazamiento residual:

Verificación técnica:
    Espectros antes/después:
    Aceleración, velocidad y desplazamiento antes/después:
    Intensidad de Arias acumulada:
    FAS o espectrograma:
    Justificación de aceptación/rechazo:
```

### 26.25 Resumen operacional

SeismoMatch debe entenderse como una implementación práctica de spectral matching no estacionario en dominio del tiempo. Su mecánica esencial es ensamblar una matriz de sensibilidad entre wavelets de corrección y respuestas máximas de osciladores SDOF, resolver las amplitudes por un sistema lineal regularizado, actualizar el acelerograma y repetir hasta cumplir la tolerancia. La opción moderna basada en Al Atik & Abrahamson usa una wavelet de coseno taperizado con envolvente gaussiana que busca preservar la no estacionariedad y evitar deriva en velocidad/desplazamiento.

La calidad del resultado depende menos de lograr un error espectral muy pequeño y más de un equilibrio entre compatibilidad espectral, plausibilidad física, conservación de duración/energía y coherencia con el escenario de amenaza. En aplicaciones geotécnicas, el punto de control del movimiento y la compatibilidad con respuesta de sitio son tan importantes como el matching espectral.


## 27. Fuentes técnicas consultadas

Las siguientes fuentes sustentan la metodología, criterios y algoritmos descritos:

1. **NIST GCR 11-917-15.** *Selecting and Scaling Earthquake Ground Motions for Performing Response-History Analyses.* NEHRP Consultants Joint Venture, 2011.  
   https://www.nehrp.gov/pdf/nistgcr11-917-15.pdf

2. **Al Atik, L. & Abrahamson, N. (2010).** *An Improved Method for Nonstationary Spectral Matching.* Earthquake Spectra, 26(3), 601–617.  
   https://doi.org/10.1193/1.3459159

3. **Hancock, J., Watson-Lamprey, J., Abrahamson, N., Bommer, J., Markatis, A., McCoyh, E., & Mendis, R. (2006).** *An Improved Method of Matching Response Spectra of Recorded Earthquake Ground Motion Using Wavelets.* Journal of Earthquake Engineering, 10(sup001), 67–89.  
   https://doi.org/10.1080/13632460609350629

4. **RSPMatch09 User Manual.** *Time-domain spectral matching by adding adjustment wavelets.*  
   https://ia601809.us.archive.org/20/items/manualzilla-id-5791287/5791287.pdf

5. **SeismoSoft.** *SeismoMatch Help: Spectral Matching.*  
   https://help.seismosoft.com/seismomatch/2025/loading_%26_preprocessing/spectral_matching.htm

6. **SeismoSoft.** *SeismoMatch product documentation: wavelet-based matching and quality control.*  
   https://seismosoft.com/products/seismomatch/

7. **Boore, D. M. & Bommer, J. J. (2005).** *Processing of strong-motion accelerograms: needs, options and consequences.* Soil Dynamics and Earthquake Engineering, 25(2), 93–115.  
   https://doi.org/10.1016/j.soildyn.2004.10.007

8. **PEER.** *Strong Ground Motion Databases: NGA-East and NGA-West2.*  
   https://peer.berkeley.edu/peer-strong-ground-motion-databases

9. **Baker, J. W. (2011).** *Conditional Mean Spectrum: Tool for Ground-Motion Selection.* Journal of Structural Engineering, 137(3), 322–331.  
   https://doi.org/10.1061/(ASCE)ST.1943-541X.0000215

10. **Baker, J. W. & Jayaram, N. (2008).** *Correlation of Spectral Acceleration Values from NGA Ground Motion Models.* Earthquake Spectra, 24(1), 299–317.  
    https://doi.org/10.1193/1.2857544

11. **Haselton, C. B. et al. (2017).** *Response-History Analysis for the Design of New Buildings in the NEHRP Provisions and ASCE/SEI 7 Standard: Part I.* Earthquake Spectra, 33(2), 373–395.  
    https://doi.org/10.1193/032114EQS039M

12. **Rocscience RSSeismic Documentation.** *Response Spectra Calculation Methods.*  
    https://www.rocscience.com/help/rsseismic/documentation/motions/response-spectra-calculation-methods

13. **SeismoSoft SeismoSignal Documentation.** *Ground Motion Parameters: Arias, durations, CAV, spectra.*  
    https://help.seismosoft.com/seismosignal/2025/spectra_%26_ground_motion_parameters/ground_motion_parameters.htm

14. **Montejo/Suarez CWT methodology.** *An improved CWT-based algorithm for the generation of spectrum-compatible records.* International Journal of Advanced Structural Engineering, 2013.  
    https://link.springer.com/article/10.1186/2008-6695-5-26

15. **Harati et al.** *Non-Stationary Spectral Matching by Unconstrained Optimization and Wavelet Analysis: Theory and Applications in Structural Dynamics.*  
    https://arxiv.org/abs/1905.02394

16. **REQPY.** *Python module for spectral matching using CWT, PSA, FAS and PSD compatibility.*  
    https://github.com/LuisMontejo/REQPY

17. **ASCE/SEI technical discussion.** *ASCE 7-22 Ground Motions: Multi-Period Design Response Spectrum.*  
    https://www.asce.org/communities/institutes-and-technical-groups/structural-engineering-institute/news/asce-7-22-ground-motions---a-rational-approach-for-structural-engineers

18. **Rocscience RSSeismic.** *Deconvolution via Frequency Domain Analysis.*  
    https://www.rocscience.com/help/rsseismic/documentation/properties/layer-properties/deconvolution-via-frequency-domain-analysis


19. **SeismoSoft (2026).** *SeismoMatch Help System — About SeismoMatch.*  
    https://help.seismosoft.com/seismomatch/2026/about_seismomatch/about_seismomatch.htm

20. **SeismoSoft (2026).** *SeismoMatch Help System — Spectral matching.*  
    https://help.seismosoft.com/seismomatch/2026/loading_%26_preprocessing/spectral_matching.htm

21. **SeismoSoft (2026).** *SeismoMatch Help System — Elastic response spectra.*  
    https://help.seismosoft.com/seismomatch/2026/response_spectra_%26_ground_motion_parameters/elastic_response_spectra.htm

22. **SeismoSoft (2026).** *SeismoMatch Help System — Defining the target spectrum.*  
    https://help.seismosoft.com/seismomatch/2026/loading_%26_preprocessing/defining_the_target_spectrum.htm

23. **SeismoSoft (2026).** *SeismoMatch Help System — Ground motion parameters.*  
    https://help.seismosoft.com/seismomatch/2026/response_spectra_%26_ground_motion_parameters/ground_motion_parameters.htm

24. **Al Atik, L. & Abrahamson, N. (2010).** *An Improved Method for Nonstationary Spectral Matching.* Earthquake Spectra, 26(3), 601–617.  
    https://doi.org/10.1193/1.3459159

25. **RSPMatch09 Manual.** *User Manual for RSPMatch09.*  
    https://ia601809.us.archive.org/20/items/manualzilla-id-5791287/5791287.pdf

26. **Hancock, J., Watson-Lamprey, J., Abrahamson, N. A., Bommer, J. J., Markatis, A., McCoy, E. & Mendis, R. (2006).** *An improved method of matching response spectra of recorded earthquake ground motion using wavelets.* Journal of Earthquake Engineering, 10(sup001), 67–89.  
    https://doi.org/10.1080/13632460609350629

---

## 28. Plantilla rápida para especificación de proyecto

```text
Proyecto:
Ubicación:
Nivel de amenaza:
Norma / criterio:
Tipo de análisis:
Ubicación del movimiento objetivo:
Espectro objetivo:
Amortiguamiento:
Rango de periodos:
Número de registros:
Base de datos:
Criterios de selección:
    Magnitud:
    Distancia:
    Mecanismo:
    Vs30 / clase de sitio:
    Directividad:
    Duración:
Método:
    Escalamiento lineal:
    Ajuste espectral:
    Algoritmo wavelet:
    Tolerancia:
Control de calidad:
    PGA:
    PGV:
    PGD:
    Arias:
    D5-95:
    FAS/PSD:
    Velocidad/desplazamiento residual:
    Pulso:
Entregables:
    Acelerogramas finales
    Espectros
    Tablas de escala
    Figuras comparativas
    Parámetros de control
    Scripts/configuración
```

---

## 29. Glosario de símbolos

| Símbolo | Significado |
|---|---|
| $a_g(t)$ | aceleración del suelo |
| $u(t)$ | desplazamiento relativo del oscilador |
| $\dot u(t)$ | velocidad relativa |
| $\ddot u(t)$ | aceleración relativa |
| $m$ | masa |
| $c$ | amortiguamiento viscoso |
| $k$ | rigidez |
| $\xi$ | razón de amortiguamiento |
| $\omega_n$ | frecuencia circular natural |
| $T$ | periodo natural |
| $S_a$ | aceleración espectral |
| $S_d$ | desplazamiento espectral |
| $PSA$ | pseudo-aceleración espectral |
| $I_A$ | intensidad de Arias |
| $D_{5-95}$ | duración significativa 5%-95% |
| $\mu$ | ductilidad |
| $F_y$ | resistencia de fluencia |
| $u_y$ | desplazamiento de fluencia |
| $R_\mu$ | factor de reducción por ductilidad |
| $s$ | factor de escala lineal |
| $f_j(t)$ | wavelet de ajuste |
| $b_j$ | amplitud de wavelet |
| $C_{ij}$ | matriz de sensibilidad espectral |
| $\lambda$ | relajación o regularización |
| $T^*$ | periodo de condicionamiento |
| $\rho_{i*}$ | correlación entre ordenadas espectrales |
| $V_{S30}$ | velocidad promedio de onda cortante en 30 m |

